# Especificação Oficial do Círcula

Este documento é a referência funcional oficial do produto Círcula. Todo desenvolvimento futuro deve
ser avaliado em relação a este conteúdo. Não simplificar, não descaracterizar e não substituir estas
ideias por funcionalidades genéricas sem marcação explícita como sugestão futura.

## 1. Conceito da Plataforma

A plataforma é uma rede social de comunidades voltada para mulheres.

Cada profissional pode criar sua própria comunidade e convidar mulheres para participar.

Exemplos:

- Nutricionista → comunidade de alimentação e bem-estar
- Personal trainer → comunidade de fitness
- Psicóloga → comunidade de desenvolvimento pessoal
- Professora → comunidade de estudos
- Empreendedora → comunidade de negócios para mulheres

A profissional é a criadora e anfitriã da comunidade, mas não deve ser o centro da interação.

O principal objetivo da comunidade é:

Permitir que as próprias mulheres conversem, compartilhem experiências, se apoiem e criem vínculos entre si.

A profissional cria o ambiente, define os temas e participa quando desejar.

## 2. Exemplo

Primeira comunidade:

**Fluir e Florescer**

Criadora: Nutricionista Marluce

Mensalidade: R$ 14,90/mês

As mulheres pagam uma assinatura recorrente para participar da comunidade.

Dentro dela podem:

- criar publicações
- comentar
- reagir
- conversar
- compartilhar experiências
- participar de desafios
- participar de grupos/círculos
- responder perguntas
- acompanhar atividades
- conhecer outras participantes

## 3. Princípio Fundamental

A comunidade não deve funcionar como um grupo de conteúdo da profissional.

O modelo deve ser:

Profissional → cria e organiza o ambiente

Aplicativo → estimula a interação

Mulheres → criam a maior parte das conversas

Exemplo:

Ana publica:

> "Estou tentando voltar a fazer exercícios depois de muito tempo parada."

Carla responde:

> "Eu também! Comecei há duas semanas."

Juliana:

> "Comigo aconteceu a mesma coisa."

Isso gera uma conversa espontânea entre as participantes.

## 4. Automações sem IA

NÃO utilizar inteligência artificial para gerar conteúdo ou conversar com as participantes.

Toda automação deve funcionar através de:

- comandos predefinidos
- perguntas cadastradas
- desafios cadastrados
- mensagens cadastradas
- regras de tempo
- sorteio aleatório
- categorias
- interesses
- eventos
- atividades programadas pela dona da comunidade

A dona da comunidade deve conseguir cadastrar previamente o conteúdo utilizado pelo sistema.

## 5. Pergunta do Dia

A dona da comunidade poderá cadastrar várias perguntas.

Exemplos:

- Qual foi uma pequena vitória sua esta semana?
- O que você fez por você hoje?
- Qual hábito você está tentando criar?
- O que está sendo mais difícil para você ultimamente?
- Qual conselho você daria para uma mulher que está começando?
- Compartilhe algo bom que aconteceu esta semana.

O sistema armazenará essas perguntas.

Todos os dias, ou em horários configuráveis, poderá selecionar aleatoriamente uma pergunta.

Exemplo:

> **PERGUNTA DO DIA**
>
> Qual foi uma pequena vitória sua esta semana?
>
> Conte para as outras mulheres.

A pergunta aparece no feed como uma publicação especial da comunidade.

## 6. Sistema de Desafios

A dona da comunidade poderá criar desafios.

Exemplo:

**Desafio: 7 dias cuidando de mim**

Duração: 7 dias.

Atividades:

- Dia 1 — Faça algo que você gosta.
- Dia 2 — Beba mais água.
- Dia 3 — Faça uma caminhada.
- Dia 4 — Reserve 15 minutos para você.
- Dia 5 — Compartilhe uma pequena conquista.
- Dia 6 — Conte algo que você aprendeu.
- Dia 7 — Conte como foi participar do desafio.

O sistema libera automaticamente uma atividade por dia.

As participantes podem marcar "Concluído" e acompanhar seu progresso.

## 7. Desafios Coletivos

Os desafios devem mostrar quantas mulheres estão participando.

Exemplo:

> Desafio 7 dias cuidando de mim
>
> 86 mulheres participando
>
> 43 fizeram o desafio de hoje.

Isso cria sensação de comunidade.

Também deve existir uma área de comentários para as participantes conversarem durante o desafio.

## 8. Círculos de Interesse

A comunidade poderá ter pequenos grupos internos chamados Círculos.

Exemplos:

- Começando agora
- Mulheres que querem voltar a se exercitar
- Alimentação saudável
- Mulheres 40+
- Quero cuidar mais de mim
- Mães
- Mulheres empreendedoras

A dona da comunidade poderá criar os círculos manualmente.

A participante poderá escolher quais deseja participar.

## 9. Sugestão de Conexões

O aplicativo poderá sugerir que uma mulher conheça outra participante com interesses semelhantes.

NÃO utilizar IA. Utilizar somente informações cadastradas pela própria usuária.

Exemplo:

Ana: Fitness, Alimentação, Corrida, Bem-estar

Carla: Corrida, Fitness, Bem-estar

O aplicativo poderá mostrar:

> "Vocês têm interesses em comum"
>
> "Ana, conheça Carla."
>
> "Vocês também fazem parte do grupo de corrida."

A participante poderá:

- Ver perfil
- Seguir
- Enviar mensagem, caso essa funcionalidade esteja habilitada

## 10. Reconhecimento das Participantes

A plataforma deve reconhecer mulheres que contribuem positivamente para a comunidade.

Exemplo: **Membro que Floresce**

Pode ser concedido quando a participante:

- ajuda outras mulheres
- participa frequentemente
- responde publicações
- participa de desafios
- recebe reações positivas
- contribui para a comunidade

A classificação deve ser baseada em regras predefinidas, não IA.

Exemplo:

- 10 comentários úteis → selo inicial
- 25 comentários → novo nível
- 50 comentários → novo nível
- 100 comentários → reconhecimento especial

A dona da comunidade poderá configurar essas regras.

## 11. Perguntas Aleatórias no Feed

Além da pergunta diária, o sistema poderá publicar perguntas aleatórias em determinados horários.

A dona cria um banco de perguntas (Pergunta 1, Pergunta 2, ... Pergunta 100). O aplicativo escolhe
aleatoriamente.

Configuração exemplo:

- Frequência: 1 por dia
- Horário: 09:00
- Dias: segunda a sexta

Ou:

- 2 vezes por semana
- Dias aleatórios
- Horários aleatórios dentro de uma faixa definida

## 12. Comandos de Engajamento

A dona poderá cadastrar comandos.

Exemplos:

- **"Compartilhe sua vitória"** — "O que você conseguiu fazer esta semana e está orgulhosa?"
- **"Momento de reflexão"** — "Pare alguns minutos e conte: como você está se sentindo hoje?"
- **"Ajude outra mulher"** — "Compartilhe uma experiência que possa ajudar outra participante."
- **"Conheça alguém"** — "Escolha uma mulher da comunidade e diga oi."
- **"Desafio rápido"** — "Faça uma pequena coisa hoje que faça bem para você."

Esses comandos podem ser selecionados aleatoriamente pelo sistema.

## 13. Sistema de Check-in

A plataforma poderá criar check-ins periódicos.

Exemplo:

> **Check-in da semana**
>
> Como você está?
>
> 😊 Estou muito bem
> 🙂 Estou bem
> 😐 Estou mais ou menos
> 😔 Estou passando por uma semana difícil

Depois: "Quer compartilhar alguma coisa com as outras mulheres?" — Sim / Agora não

O objetivo NÃO é fazer diagnóstico. É criar oportunidades para conversa e apoio.

## 14. Memória da Comunidade

Todo conteúdo criado pelas participantes permanece organizado e pesquisável.

As participantes poderão pesquisar: receitas, exercícios, corrida, motivação, experiências, rotina,
desafios, perguntas.

A pesquisa deve usar: texto, categorias, hashtags, palavras-chave, filtros. SEM IA.

## 15. Participação da Profissional

A profissional não precisa produzir conteúdo diariamente.

Ela poderá definir uma programação, por exemplo:

- Segunda: Pergunta automática
- Quarta: Desafio
- Sexta: Pergunta automática
- Domingo: Check-in

E entrar quando quiser para participar das conversas.

A presença da profissional deve ser especial, não obrigatória.

## 16. Painel da Dona da Comunidade

A profissional terá um painel para administrar sua comunidade.

**Conteúdo:**
- criar perguntas
- criar desafios
- criar comandos
- criar categorias
- criar círculos
- criar eventos
- criar conteúdos
- programar publicações

**Comunidade:**
- número de membros
- novas participantes
- participantes ativas
- participantes inativas
- posts
- comentários
- reações
- desafios concluídos

**Assinaturas:**
- assinantes ativos
- cancelamentos
- novas assinaturas
- receita mensal
- produtos vendidos

## 17. Monetização

A comunidade poderá ter assinatura.

Exemplo: Fluir e Florescer — R$ 14,90/mês, cobrança recorrente no cartão.

A profissional também poderá vender dentro da comunidade: cursos, ebooks, workshops, eventos,
programas, aulas, consultas, produtos físicos, outros produtos permitidos pela plataforma.

A plataforma poderá receber uma porcentagem das vendas.

## 18. Princípio de Design

A experiência visual deve transmitir: **acolhimento + pertencimento + conexão + confiança + leveza**.

NÃO criar interface parecida com: Facebook, LinkedIn, dashboard corporativo, fórum tradicional.

A experiência deve parecer uma comunidade viva.

O feed deve privilegiar: pessoas + histórias + conversas + experiências — e não apenas conteúdo
profissional.

## 19. Princípio Mais Importante

A dinâmica deve ser:

```
PROFISSIONAL
    ↓ cria o ambiente
APLICATIVO
    ↓ estimula interações
MULHERES
    ↕ conversam, ajudam, compartilham, se conectam
```

A profissional não é o produto. **A comunidade é o produto.**

A profissional fornece curadoria, confiança, organização e conhecimento especializado.

As mulheres fornecem a vida da comunidade.

## Regra Absoluta para o Desenvolvimento

NÃO utilizar IA para gerar:

- perguntas
- respostas
- aconselhamentos
- conteúdos
- conversas automáticas

Toda automação deverá trabalhar com:

- conteúdos previamente cadastrados
- perguntas previamente cadastradas
- desafios previamente cadastrados
- comandos previamente cadastrados
- mensagens previamente cadastradas
- regras de tempo
- sorteios aleatórios
- categorias
- interesses
- eventos
- atividades programadas
- regras de participação

O sistema pode escolher conteúdos aleatoriamente, programar ciclos, aplicar regras, sugerir conexões
por interesses cadastrados e acompanhar métricas.

A automação existe para **ESTIMULAR** a interação humana. Ela não deve **SUBSTITUIR** a interação
humana.

## Roadmap do MVP

**FASE 1**
- autenticação
- usuário Master
- profissional
- primeira comunidade
- membros

**FASE 2**
- perfil
- feed
- posts
- comentários
- reações

**FASE 3**
- perguntas
- desafios
- círculos
- check-ins
- comandos de engajamento

**FASE 4**
- painel da profissional
- métricas
- assinaturas
- monetização

**FASE 5**
- expansão para outras profissionais
- múltiplas comunidades
- recursos avançados

## Estado Atual do Projeto (última atualização: 2026-08-21)

### Concluído

**Autenticação (FASE 1, parte 1):**
- Login com e-mail/senha, sessão persistida, fluxo "Esqueci minha senha" (`resetPasswordForEmail` + tela de nova senha), tudo funcionando e testado de ponta a ponta no navegador.
- Usuário Master (`jorgesoaresjunior77@gmail.com`, UUID `18004064-4776-4c12-8f9f-b1bae6c390f5`) com `role = master`, confirmado.

**Comunidades (FASE 1, parte 2) — CONCLUÍDA E VALIDADA DE PONTA A PONTA:**
- Schema já existente (`profiles`, `communities`, `community_members`) foi suficiente — nenhuma migration nova foi necessária nesta etapa.
- Implementados: `src/types/community.ts`, `src/hooks/useCommunity.ts`, `src/components/CreateCommunityForm.tsx`, `src/components/CommunityView.tsx`, `src/components/MemberList.tsx`, e `src/components/Dashboard.tsx` ajustado para tratar `master` / `professional` / `member` de forma diferente.
- Primeira profissional real cadastrada: **Marluce** (`marlucebassaninutri@gmail.com`, UUID `1c20d81a-1312-4bdd-9e40-390a81536fd1`), `role = professional`.
- Primeira comunidade real, **Fluir & Florescer**, foi criada **pelo próprio Círcula** (pelo `CreateCommunityForm`, logada como Marluce) — não por INSERT manual. Confirmado via consulta real ao banco: `id 077aeceb-7321-48ca-8c23-cb256823755a`, `owner_id` = UUID da Marluce, nome e descrição corretos.
- Testado ao vivo no navegador: Master vê a área de comunidades (lista geral), Professional vê sua própria comunidade + formulário de criação (quando ainda não tem uma), Member (estrutural, via RLS) só veria a comunidade a que pertence. Nenhum erro de RLS, permissão ou console em nenhum dos testes.

## Etapa concluída: Membros da comunidade

- A Professional Marluce conseguiu acessar a comunidade Fluir & Florescer.
- A funcionalidade "Adicionar mulher" foi implementada e testada.
- A busca é realizada pelo e-mail de uma conta já existente no Círcula.
- A conta de teste foi encontrada corretamente.
- A conta foi adicionada à comunidade com sucesso.
- O profile_id confirmado foi:
  94bc64f8-3ecc-42da-84c2-abfcbc3f80ef
- O status do membro ficou:
  active
- A MemberList foi atualizada automaticamente sem recarregar a página.
- A comunidade passou de 0 para 1 mulher.
- A tentativa de adicionar novamente a mesma mulher foi bloqueada corretamente.
- Nenhuma duplicata foi criada.
- A mensagem "Essa mulher já faz parte desta comunidade." foi exibida.
- Não houve erros no console.
- Não houve erros de permissão/RLS durante o teste.
- O fluxo foi validado no navegador com a conta real da Professional.

A funcionalidade de membros está considerada **concluída** para esta etapa.

### Gotcha de infraestrutura descoberto e resolvido nesta fase (relevante para o futuro)

Tabelas criadas via migration SQL direta (fora do editor visual do Supabase Studio) **não recebem GRANT automático** para o role `authenticated`, mesmo com RLS/policies corretas. Isso já causou 403 em `profiles`, depois em `communities`/`community_members`. Se uma tabela nova for criada no futuro (ex.: `posts`, `questions`, `challenges`), rodar sempre, no SQL Editor:
```sql
GRANT SELECT, INSERT ON public.<tabela> TO authenticated;
-- + UPDATE/DELETE conforme a policy exigir
```
antes de testar a feature, para não repetir esse diagnóstico do zero.

Também existe uma trigger `profiles_prevent_role_escalation` que reverte silenciosamente qualquer `UPDATE` de `role` feito fora de um contexto autenticado (`auth.uid()` nulo, como o SQL Editor). Para promover um profile manualmente (bootstrap), o padrão seguro usado duas vezes com sucesso é:
```sql
begin;
alter table public.profiles disable trigger profiles_prevent_role_escalation;
update public.profiles set role = '<nova_role>' where id = '<uuid>';
alter table public.profiles enable trigger profiles_prevent_role_escalation;
commit;
```

### Pendências / observações para retomar

- `public.profiles.full_name` está `NULL` para Master e para Marluce — o Dashboard mostra "Olá, usuária" como fallback. Combinado explicitamente para ser corrigido em uma etapa futura de perfil (FASE 2), não agora.
- `community_members` está vazia — ainda não existe fluxo de adicionar/convidar membro nem de uma mulher se juntar a uma comunidade. Isso é o próximo pedaço natural da FASE 1 ("membros") antes de avançar para FASE 2 (feed/posts).
- Nenhuma migration nova foi criada em toda esta etapa — apenas os `GRANT`s (fora de migration, rodados manualmente) e o `UPDATE` de role da Marluce.
- Nada foi commitado nem enviado ao GitHub em nenhum momento desta fase — o repositório local está à frente do `origin/main` só pelo commit anterior à sessão de auth; tudo desenvolvido depois (auth fixes locais + Comunidades) está sem commit, aguardando autorização.

### Próxima etapa recomendada

Completar o restante da FASE 1: fluxo de **membros** — uma forma (pelo painel da profissional, ou por convite) de associar mulheres existentes a uma comunidade via `community_members`, respeitando a policy já existente (`community_members_insert`: só `is_master()` ou dono da comunidade). Só depois disso faz sentido avançar para FASE 2 (feed/posts/comentários/reações).

## Experiência de Comunidades — concluída

- `CommunityView` redesenhado como cartão de comunidade.
- Avatar da comunidade.
- Identificação clara do vínculo do usuário:
  - "Você é a anfitriã desta comunidade"
  - "Você participa desta comunidade"
- Contagem de participantes exibida somente quando os dados disponíveis pela RLS permitem uma contagem confiável.
- Member não recebe uma contagem potencialmente enganosa.
- `MemberList` simplificada para "Participantes".
- Dashboard passou a utilizar os rótulos:
  - "Minha comunidade" para Professional/Member.
  - "Comunidades" para Master.
- Master, Professional e Member foram testados no navegador.
- Console sem erros.
- Build aprovado.
- Nenhum commit realizado.
- Nenhum push realizado.

### Pendências futuras desta etapa

- Descoberta de outras comunidades (hoje só é possível ver a comunidade a que já se pertence ou administra).
- Entrada/autosserviço em comunidades (hoje só Master ou a dona da comunidade podem adicionar uma participante).
- Eventual revisão da RLS necessária para suportar essas duas funcionalidades acima.

## Descoberta e entrada em comunidades — concluída tecnicamente

- Coluna `communities.is_discoverable` criada (`default true`).
- Policy `communities_select` estendida para incluir comunidades `is_discoverable = true`.
- Policy `community_members_insert` estendida para permitir autosserviço (a própria pessoa se adicionar) somente em comunidades descobríveis.
- Função `community_member_count(p_community_id)` criada (`security definer`) para contagem confiável de participantes, independente do que a RLS deixaria ver por linha.
- RLS do autosserviço foi **validada com teste real**: uma tentativa de auto-inserção numa comunidade da qual a conta de teste já era membro retornou `23505` (violação de constraint única), não `42501` (permissão negada) — prova de que a policy libera o `INSERT` antes da constraint barrar a duplicata.
- A constraint `unique (community_id, profile_id)` foi validada no mesmo teste.
- `community_member_count` foi validada com chamada real (retornou `2`, batendo com as linhas reais).
- A Professional (Marluce) agora também possui uma linha própria em `community_members` para a sua comunidade (via backfill único desta etapa) — o que também resolve, de forma colateral, a visibilidade do perfil dela para quem compartilha a comunidade.
- `createCommunity()` foi preparado para, a partir de agora, criar automaticamente o vínculo dona+membro sempre que uma nova comunidade for criada.
- Código React implementado: `useCommunity.ts` (`joinCommunity`, `memberCounts` via RPC), `CommunityView.tsx` (botão "Entrar", badge "Disponível para descobrir"), `Dashboard.tsx` (Member agora separa "Minha comunidade" de "Descobrir comunidades", sem assumir uma única comunidade).
- `npm run build` passou.
- Master, Professional e Member foram testados no navegador, sem erros de console.

**Limitação de teste registrada explicitamente:** o fluxo visual completo **"Descobrir comunidade → Entrar → mover para Minha comunidade"** ainda **não foi exercitado ao vivo**, porque atualmente existe apenas uma comunidade em todo o sistema e o único Member de teste já participa dela — não há hoje nenhuma comunidade "descobrível e ainda não participada" disponível para clicar em "Entrar" e observar o card se mover. A validação foi feita por: (a) prova real de RLS/constraint no banco, e (b) revisão do código, que reutiliza exatamente o mesmo padrão de `insert` já comprovado. O teste visual de ponta a ponta fica pendente para quando existir uma segunda comunidade real (ex.: uma segunda profissional).

## Identidade visual oficial aplicada — concluída

A pasta `identidadevisual/` passou a conter os arquivos originais organizados da marca: `Logo.png` (logotipo completo), `Icone.png` (símbolo isolado) e `Painel.png` (referência de acabamento visual, estilo "Orkut moderno" — usada só como inspiração de organização/espaçamento, não como especificação funcional).

- `src/assets/circula-icon.png` e `src/assets/circula-logo.png` recortados diretamente das artes oficiais (não foram redesenhados nem recriados com CSS/texto).
- `public/favicon.png` — favicon do navegador trocado do padrão do Vite para o ícone oficial do Círcula.
- Tela de login (`Login.tsx`) passou a exibir a **logo completa em destaque** na entrada principal; a tela de recuperação de senha manteve o lockup pequeno (ícone + texto).
- Paleta de cores confirmada fiel ao original por amostragem de pixel direto no `Logo.png` (vinho da wordmark bateu em `#5B2433`, praticamente idêntico ao `#5B2E3F` já usado).
- Cards de comunidade receberam raio maior e sombra suave, aproximando o acabamento da referência de painel, sem copiar nenhuma funcionalidade que só existe no mockup (scraps, amigas, fóruns, eventos, mensagens, depoimentos).
- **Fontes:** não há nenhum arquivo `.woff`/`.woff2`/`.ttf`/`.otf` em `identidadevisual/`. A tipografia permanece `system-ui` — não foi trocada por suposição. Fica pendente uma decisão futura sobre fonte oficial, caso o arquivo da fonte seja disponibilizado.
- Testado no navegador (Login, Recuperar senha, Dashboard como Professional e Master): favicon confirmado via DOM, sem erros de console, sem regressão funcional.
- `npm run build` aprovado.
- Nenhuma alteração de banco, Auth, RLS, policies, roles, Storage ou regra de negócio nesta etapa.
- Nenhum commit, nenhum push.

## Etapa concluída: Responsividade e identidade visual

- Login testado em 320px, 375px, 390px, 414px, 768px, 1024px, 1280px e 1440px.
- Não houve scroll horizontal no Login.
- Logo responsivo funcionando.
- Botão Entrar e link de recuperação funcionando.
- Dashboard Professional testado em desktop e mobile.
- Grid de 2 colunas confirmado em 1024px.
- Mobile reorganizado em uma coluna.
- Dashboard Member testado em desktop e 375px.
- Dashboard Master testado em desktop e 375px.
- ProfileCard testado.
- Edição de perfil testada nas 8 larguras.
- Problema de overflow encontrado na edição de perfil foi corrigido.
- Console sem erros.
- `npm run build` passou.
- Favicon e assets oficiais do Círcula foram incorporados.
- A identidade visual utiliza os assets encontrados na pasta `identidadevisual/`.
- O layout foi adaptado para desktop, notebook, tablet e mobile.

A referência visual do dashboard (`identidadevisual/Painel.png`) continua sendo apenas referência estética e de organização, não uma especificação de funcionalidades.

## Etapa concluída: FASE 2a — Posts + Feed

- `posts` implementados.
- `Feed` implementado.
- `PostComposer` implementado.
- `PostCard` implementado.
- Professional pode publicar.
- Member pode publicar.
- Master pode visualizar, mas não publicar.
- Autoria exibida corretamente.
- Avatar e nome utilizados no post.
- Posts aparecem do mais recente para o mais antigo.
- Feed não aparece em "Descobrir comunidades".
- Proteção por RLS validada.
- Acesso sem sessão bloqueado.
- Testes reais com Professional, Member e Master realizados.
- Responsividade testada nas larguras de 320px a 1440px.
- Console sem erros.
- `npm run build` passou.

**Limitação registrada:** o teste negativo de isolamento entre duas comunidades ainda não foi realizado ao vivo porque existe apenas uma comunidade real no banco. A proteção está implementada por RLS usando `is_community_member()`, `owns_community()` e `is_master()`, já validadas anteriormente. O teste entre comunidades será realizado quando existir uma segunda comunidade real.

## Etapa concluída: FASE 2b — Comentários + Reações

- `post_comments` implementados.
- `post_reactions` implementados.
- `CommentList` implementado.
- `CommentForm` implementado.
- `PostCard` atualizado com reação e comentários inline.
- Professional pode comentar e reagir.
- Member pode comentar e reagir.
- Master pode visualizar comentários e reações, mas não comentar nem reagir.
- Contagem de comentários e reações funcionando.
- Retirada da própria reação funcionando (toggle).
- Atualização da interface sem reload.
- Comentários abrem inline dentro do `PostCard` (sem nova rota).
- RLS validada com probes reais (Master bloqueado com 403 "row-level security policy", grants confirmados corretos).
- Testes reais com Professional (Marluce), Member e Master realizados.
- Responsividade testada de 320px a 1440px, sem scroll horizontal.
- Console sem erros.
- `npm run build` passou.

**Limitação registrada:** o teste negativo de isolamento entre duas comunidades para comentários e reações não foi realizado ao vivo, pelo mesmo motivo já registrado na FASE 2a — existe apenas uma comunidade real no banco. A proteção herda a mesma base de RLS (`can_view_post()` e `can_participate_in_post()`, construídas sobre `is_community_member()`, `owns_community()` e `is_master()`, já validadas anteriormente). O teste entre comunidades será realizado quando existir uma segunda comunidade real.

## Etapa concluída: FASE 3 — Pergunta do Dia

- `community_questions` implementada (RLS, policies e GRANTs verificados por leitura no catálogo do Postgres, com o próprio usuário rodando as consultas).
- `posts` estendida com `post_type` e `question_id`, preservando o histórico mesmo se a pergunta original for editada ou excluída (conteúdo copiado no momento da publicação).
- Função `publish_daily_question(uuid)` (existência e `GRANT EXECUTE` para `authenticated` confirmados por leitura no catálogo).
- `useQuestions` implementado (criar, editar, ativar/desativar, excluir, publicar).
- `QuestionBankManager` implementado.
- `PostCard` atualizado com o selo "PERGUNTA DO DIA" para publicações desse tipo, mantendo comentários e reações normalmente.
- Professional administra o banco de perguntas da própria comunidade: cria, edita, ativa, desativa, exclui e publica a pergunta do dia manualmente pelo botão "Publicar pergunta do dia agora".
- Member visualiza a pergunta do dia no feed (com o selo) e pode comentar e reagir normalmente; não tem acesso ao banco de perguntas (nem na interface, nem por RLS — validado com probe real: `select` como Member retornou `200` com `0` linhas).
- Master visualiza o banco de perguntas da comunidade (somente leitura, sem os controles de criar/editar/publicar) e visualiza a pergunta publicada no feed; continua sem poder comentar ou reagir (botões desabilitados, sem formulário de comentário).
- Publicar sem nenhuma pergunta ativa é bloqueado no cliente (botão desabilitado) antes mesmo de chegar ao banco.
- Excluir a pergunta que originou uma publicação foi testado ao vivo: o post no feed manteve o selo e o texto original intactos.
- Nenhuma nova rota ou tela foi criada — a pergunta do dia aparece inline no feed já existente.
- Agendamento automático não foi implementado nesta etapa, conforme combinado; a publicação continua manual.
- Testes reais com Professional (Marluce), Member e Master realizados no navegador.
- Responsividade testada de 320px a 1440px, sem scroll horizontal, incluindo o painel de administração com múltiplos botões (quebra em `flex-wrap`, sem overflow).
- Console sem erros.
- `npm run build` passou.

**Limitação registrada:** o teste negativo de isolamento do banco de perguntas entre duas comunidades não foi realizado ao vivo, pelo mesmo motivo já registrado nas fases anteriores — existe apenas uma comunidade real no banco. A proteção reaproveita o mesmo padrão de RLS (`owns_community()`/`is_master()`) já validado, mas o teste cruzado real fica pendente para quando existir uma segunda comunidade.

## Etapa concluída: FASE 3 — Desafios (Parte A: mecanismo + números coletivos)

- `community_challenges`, `challenge_activities`, `challenge_participants` e `challenge_progress` implementadas, com RLS, policies e GRANTs verificados por leitura no catálogo do Postgres.
- Funções `can_view_challenge`, `can_participate_in_challenge`, `challenge_current_day`, `challenge_participant_count` e `challenge_today_completed_count` implementadas e verificadas.
- `useChallenges` implementado (listar, criar, editar, ativar/desativar, excluir, entrar, sair, contagens coletivas).
- `useChallengeProgress` implementado (progresso da própria participante, marcar/desmarcar, respeitando o dia liberado).
- `ChallengeManager` implementado (administração para Professional; leitura para Master; ausente para Member).
- `ChallengeCard` implementado (título, descrição, "Dia X de Y", atividade do dia, contagens coletivas, participar, checklist de dias liberados/bloqueados com toggle "Concluído").
- Calendário compartilhado por toda a comunidade: Dia 1 = data de criação do desafio, dias seguintes liberados automaticamente conforme o tempo passa, calculado por `challenge_current_day` tanto no cliente quanto no banco.
- Professional cria, edita, ativa/desativa e exclui desafios da própria comunidade, e também participa como qualquer membro.
- Member vê os desafios ativos, entra, acompanha o próprio progresso e as contagens coletivas; não administra.
- Master visualiza os desafios e as contagens coletivas; não participa, não marca progresso, não administra.
- Nenhuma lista nominal de quem concluiu é exibida — apenas contagens agregadas vindas das funções do banco.
- Dias futuros bloqueados tanto na interface (botões desabilitados) quanto no banco (RLS validada com probe real: tentativa de marcar um dia futuro retornou `403 row-level security policy`, nenhuma linha criada).
- Privacidade entre participantes validada com probes reais: como Member, uma consulta a `challenge_progress` sem filtro retornou apenas a própria linha; uma tentativa de inserir progresso em nome de outra participante retornou `403`.
- Probes adicionais confirmaram que Member e Master não conseguem criar desafios via API mesmo contornando a interface (`403` em ambos os casos).
- Desafio fica em seção própria da comunidade ("Desafios da comunidade"), sem virar publicação no feed.
- Testes reais com Professional (Marluce), Member e Master realizados no navegador: criar, editar, ativar/desativar, excluir, entrar, marcar/desmarcar progresso, contagens coletivas corretas.
- Responsividade testada de 320px a 1440px, sem scroll horizontal, incluindo o checklist de dias e o formulário de criação com múltiplas atividades.
- Console sem erros.
- `npm run build` passou.

**Limitação registrada:** o teste negativo de isolamento de desafios entre duas comunidades não foi realizado ao vivo, pelo mesmo motivo já registrado nas fases anteriores — existe apenas uma comunidade real no banco. A área de comentários do desafio (Parte B) e o restante dos recursos de "Desafios Coletivos" descritos na seção 7 além das contagens (como notificações) ficam para uma subetapa futura.

## Etapa concluída: FASE 3 — Desafios (Parte B: comentários)

- `challenge_comments` implementada, com RLS, policies e GRANTs verificados por leitura no catálogo do Postgres, com o próprio usuário rodando as consultas.
- Policy de leitura reaproveita `can_view_challenge(challenge_id)`; policy de escrita exige `author_id = auth.uid()` e `can_participate_in_challenge(challenge_id)` — qualquer membro da comunidade que possa ver o desafio pode comentar, mesmo sem ter entrado no desafio.
- `useChallenges` estendido com `commentCounts`, `commentsByChallenge`, `fetchComments` e `addComment`, no mesmo padrão já usado para comentários de post.
- `ChallengeCard` ganhou um bloco de comentários inline: botão com contagem (💬), abertura/fechamento e carregamento lazy (os comentários só são buscados no banco na primeira vez que a seção é aberta).
- `CommentList` e `CommentForm` (já existentes, dos comentários de post) foram reaproveitados diretamente — nenhum componente visual novo foi criado. `CommentList` foi generalizado para aceitar qualquer comentário no mesmo formato, sem alterar sua aparência ou comportamento.
- Professional cria, edita, ativa/desativa e exclui desafios da própria comunidade, e também comenta como qualquer participante.
- Member vê os desafios ativos e comenta livremente, mesmo sem ter entrado no desafio.
- Master visualiza os comentários existentes, mas não recebe o formulário de comentário — confirmado na árvore de acessibilidade da página, não apenas visualmente.
- Comentário publicado aparece na lista sem reload, com autoria correta; a contagem (💬) é atualizada sem reload e persiste após recarregar a página.
- Testes reais com Professional (Marluce) e Member realizados no navegador: comentar, ver contagem atualizar sem reload, recarregar a página e confirmar persistência.
- Responsividade testada de 320px a 1440px. A largura de 320px foi validada via iframe same-origin (o redimensionamento real da janela não funcionou neste ambiente), confirmando por medição direta do DOM que não há overflow horizontal e que o botão de comentar tem altura confortável para toque.
- Console sem erros da aplicação em nenhuma das três sessões testadas (Professional, Member, Master).
- `npm run build` passou.

**Limitações registradas:** a tentativa de confirmar por probe de API que o Master é bloqueado pela RLS ao tentar inserir um comentário (contornando a interface) não pôde ser executada, pois o ambiente de automação bloqueou essa chamada. A proteção de RLS em si já havia sido validada diretamente no banco antes da implementação, e a interface não disponibiliza o formulário de comentário ao Master. Também permanece a limitação já conhecida de todas as fases anteriores: existe apenas uma comunidade real no banco, então o isolamento de comentários de desafio entre duas comunidades não pôde ser testado ao vivo.

## Etapa concluída: FASE 3 — Círculos de Interesse

- `community_circles` e `circle_members` implementadas, com RLS, policies, funções e GRANTs verificados por leitura no catálogo do Postgres, com o próprio usuário rodando as consultas.
- Círculo tem somente nome, comunidade, criador e data de criação — sem descrição, sem status ativo/inativo, conforme decidido explicitamente (nenhuma funcionalidade além do que está na Seção 8 da especificação).
- `can_view_circle(uuid)` e `circle_member_count(uuid)` criadas e verificadas (probes com UUID inexistente retornaram `false` e `0`, como esperado).
- `useCircles` implementado (listar com participantes aninhados, criar, renomear, excluir, entrar, sair).
- `CircleCard` implementado: nome do círculo, contagem de participantes, lista de participantes (reaproveitando o componente visual de tag/pill já usado em interesses de perfil), botão único que alterna "Participar"/"Sair".
- `CircleManager` implementado (administração para Professional; leitura para Master; ausente o formulário de criação para Member, que só participa).
- Professional cria, renomeia e exclui círculos da própria comunidade, e também participa como qualquer membro.
- Member vê os círculos, entra e sai livremente, sem aprovação da dona — autosserviço puro, igual ao padrão já usado em comunidades descobríveis e em desafios.
- A Professional **não** tem nenhum controle para adicionar ou remover manualmente uma participante de um círculo — só a própria pessoa controla sua entrada/saída, conforme decidido.
- Master visualiza os círculos e a lista de participantes, mas não tem formulário de criar, não tem botão de participar/sair, e não tem "Renomear"/"Excluir" — confirmado na interface (Master não recebe nenhum desses controles).
- Círculos não têm feed, posts, comentários ou chat próprios — ficam em seção própria da comunidade ("Círculos da comunidade"), sem nenhuma relação com o feed existente.
- Nenhum sistema visual novo foi criado: o card reaproveita a família de classes já usada em Desafios (`.challenge-item-actions`, `.challenge-save-button`, `.challenge-delete-button`) e a lista de participantes reaproveita as pills de `.interest-tag` já usadas na edição de perfil.
- Testes reais com Professional (Marluce), Member e Master realizados no navegador: criar círculo, participar, ver contagem e lista atualizarem sem reload, renomear, recarregar a página e confirmar persistência, sair do círculo.
- Responsividade testada de 320px a 1440px (sem overflow horizontal, confirmado por medição direta do DOM), respeitando a densidade visual compacta já aplicada no desktop e a experiência confortável já preservada no mobile.
- Console sem erros da aplicação em nenhum dos três papéis testados.
- `npm run build` passou.

**Limitações registradas:** a mesma de todas as fases anteriores — existe apenas uma comunidade real no banco, então o isolamento de círculos entre duas comunidades não pôde ser testado ao vivo. Adicionalmente, só existem duas contas reais participando da comunidade (Marluce e um Member de teste), então o cenário de múltiplas mulheres em múltiplos círculos diferentes foi validado no mecanismo, mas não em escala.

## Etapa concluída: FASE 3 — Check-ins

- `community_checkins` (banco reutilizável de check-ins), `checkin_instances` (check-in publicado) e `checkin_responses` (resposta individual e privada) implementadas, com RLS, policies, função e GRANTs verificados por leitura no catálogo do Postgres, com o próprio usuário rodando as consultas.
- `posts.post_type` estendido para aceitar `checkin_share`, preservando `standard` e `daily_question` — nenhuma outra tabela existente foi alterada.
- `publish_checkin(uuid)` criada (`security definer`, escolhe aleatoriamente um check-in ativo da comunidade e publica uma instância), verificada por catálogo (existência e `GRANT EXECUTE` para `authenticated`), sem ser executada durante a verificação.
- Achado durante a verificação: `anon` possui GRANTs de `REFERENCES`, `TRIGGER` e `TRUNCATE` nas tabelas novas (e também em `community_circles`, confirmando ser um padrão estrutural do projeto, não algo introduzido pelos Check-ins) — sem `SELECT`/`INSERT`/`UPDATE`/`DELETE`, portanto sem risco de leitura ou escrita anônima.
- `useCheckins` implementado (banco: criar/editar/ativar/desativar/excluir; publicar; responder; compartilhar).
- `CheckinResponseForm` implementado: seletor de humor (😊🙂😐😔), pergunta de compartilhamento (Sim/Agora não), campo de texto opcional só revelado após "Sim", botão "Compartilhar" desabilitado enquanto o texto estiver vazio.
- `CheckinManager` implementado: administração do banco e publicação para Professional; lista de check-ins publicados para todos; roster de humores individuais (nome + emoji) visível somente para a Professional.
- `PostCard` atualizado com o selo "Check-in" para publicações de compartilhamento, mantendo comentários e reações normalmente (reaproveitamento total da infraestrutura de posts já existente, sem nenhuma coluna nova).
- Professional cadastra, edita, ativa/desativa e exclui check-ins da própria comunidade, publica manualmente, responde ao próprio check-in como qualquer participante, e visualiza o humor individual de cada participante que respondeu.
- Member vê os check-ins publicados, responde uma vez por check-in (humor obrigatório, compartilhamento opcional), e só vê o próprio humor — nunca o de outra participante.
- Master visualiza os check-ins publicados (só o texto do evento), mas não tem banco, não tem seletor de humor, não tem botão de resposta e não visualiza nenhum humor individual — confirmado na interface.
- Testado ao vivo: escolher "Sim" e deixar o texto vazio mantém o botão "Compartilhar" desabilitado e não publica nada (clique sem efeito); escolher "Sim" com texto preenchido cria o post no feed com o selo "Check-in", reação e comentário funcionando normalmente; escolher "Agora não" registra a resposta sem criar nenhuma publicação.
- Testes reais com Professional (Marluce) e Member realizados no navegador, em dois check-ins publicados diferentes, cobrindo os três caminhos de resposta (Agora não / Sim com texto / Sim sem texto).
- Responsividade testada de 320px a 1440px (sem overflow horizontal, confirmado por medição direta do DOM).
- Console sem erros da aplicação em nenhum dos três papéis testados.
- `npm run build` passou.

## Etapa concluída: FASE 3 — Comandos de Engajamento

- `community_engagement_commands` (banco de comandos reutilizáveis: título + texto) implementada, com RLS, policies e GRANTs verificados por leitura no catálogo do Postgres, com o próprio usuário rodando as consultas.
- `posts.post_type` estendido para aceitar `engagement_command`, preservando `standard`, `daily_question` e `checkin_share`; `posts` ganhou as colunas `title` e `engagement_command_id` (ambas nulas, usadas só por este tipo de post) — nenhuma outra tabela existente foi alterada.
- `publish_engagement_command(uuid)` criada (`security definer`, `search_path` fixado em `public`, escolhe aleatoriamente um comando ativo da comunidade e publica um post com o selo, retornando o UUID do post criado), verificada por catálogo (existência, assinatura, tipo de retorno, `security definer`, `search_path` e `GRANT EXECUTE` para `authenticated`).
- Achado durante o desenvolvimento: uma tentativa anterior já havia criado a tabela, as colunas e uma versão da função `publish_engagement_command` com um tipo de retorno diferente; o Postgres rejeita `CREATE OR REPLACE FUNCTION` quando o tipo de retorno muda (`42P13`), então a correção precisou de `DROP FUNCTION` explícito antes de recriar. Todo o restante do schema (tabela, colunas, RLS, policies, GRANTs) já estava aplicado corretamente dessa tentativa anterior.
- `useEngagementCommands` implementado (listar, criar, editar, ativar/desativar, excluir, publicar) — mesmo padrão de `useQuestions`/`useCheckins`.
- `EngagementCommandManager` implementado: administração e publicação para Professional; leitura somente (banco de comandos com badge "Ativo"/"Inativo", sem formulário nem botões de ação) para Master; ausente para Member, que só vê o comando publicado no feed.
- `PostCard` atualizado com o selo "COMANDO DA COMUNIDADE" e o título do comando em destaque acima do texto, mantendo comentários e reações normalmente (reaproveitamento total da infraestrutura de posts já existente).
- Sem agendamento automático, sem IA, sem resposta estruturada e sem tela separada — publicação 100% manual (botão "Publicar comando agora"), igual ao padrão já usado em Pergunta do Dia e Check-ins.
- Professional cadastra, edita, ativa/desativa, exclui e publica comandos da própria comunidade, e também comenta/reage ao comando publicado como qualquer participante.
- Member vê o comando publicado no feed e comenta/reage normalmente; não tem acesso ao banco administrativo de comandos.
- Master visualiza o banco de comandos (somente leitura) e o post publicado no feed, mas não comenta nem reage — confirmado na interface (o Feed já nega comentário/reação a quem não pode postar, reaproveitando a regra existente, sem necessidade de alteração).
- Testado ao vivo com Professional (Marluce): criar, editar título/texto, desativar (botão de publicar fica bloqueado sem comando ativo), reativar, publicar (post aparece no feed sem reload, com selo e título), reagir, comentar, excluir um comando descartável criado só para esse teste.
- Testado ao vivo com Member: o comando publicado aparece no feed com o selo correto, sem o gerenciador de comandos na tela.
- Testado ao vivo com Master: banco de comandos visível em modo leitura (sem formulário/botões); clique no coração e no ícone de comentário do post publicado não altera a contagem nem abre formulário de novo comentário.
- Persistência confirmada após recarregar a página (post, reação e comentário mantidos).
- Console sem erros da aplicação em nenhum dos três papéis testados.
- `npm run build` passou.

**Limitação registrada:** desta vez não foi possível validar visualmente a responsividade de 320px a 1440px — o redimensionamento real da janela não teve efeito neste ambiente (`window.innerWidth` permaneceu em 1366px mesmo após a chamada de resize, diferente de etapas anteriores em que um workaround via iframe same-origin funcionou). A tela reaproveita integralmente classes CSS já responsivas (`.post-card`, `.question-bank`, `.question-item` etc.) e a única regra nova (`.post-title`) não usa larguras fixas, mas isso não substitui uma verificação visual real — fica pendente para quando o ambiente permitir. Mantém-se também a limitação já conhecida das fases anteriores: existe apenas uma comunidade real no banco, então o isolamento de comandos entre duas comunidades não pôde ser testado ao vivo.

**Limitações registradas:** a mesma de todas as fases anteriores — existe apenas uma comunidade real no banco, então o isolamento de check-ins entre duas comunidades não pôde ser testado ao vivo. O probe de API para confirmar por bypass de interface que o Master é bloqueado pela RLS ao tentar ler `checkin_responses.mood` não foi executado (mesma limitação do ambiente já registrada nas fases anteriores); a proteção foi validada diretamente no banco antes da implementação, e a interface do Master não oferece nenhum controle de resposta nem exibe roster de humor.

## Etapa concluída: Nova identidade visual — "leveza botânica"

Mudança puramente visual, sem tocar em banco, RLS, Auth, hooks, tipos ou lógica de nenhum componente. Objetivo: transformar a estética de "vinho/rosa saturado" para uma paleta clara, botânica e pastel (branco quente, areia, oliva, folha, rosé, palha), mantendo logo e nome "Círcula" intocados.

**Descoberta antes de alterar:** todas as cores do app já eram centralizadas em `src/index.css` (`:root`), e `src/App.css` não tinha nenhum valor de cor solto — só 3 blocos repetidos de vermelho de exclusão (`#d0244b`) e um `color: #fff` fixo. Isso permitiu aplicar a nova identidade quase inteiramente por remapeamento de tokens, sem reescrever componentes.

**Tokens remapeados em `src/index.css` (tema claro):**
- `--page-bg`: branco quente `#FEFDFC`
- `--bg`: bege quase branco `#FAF8F2` (cards de post, formulários)
- `--social-bg`: areia muito clara `#F6F2E9` (era a maior área rosa da interface — agora é a segunda maior área clara, como pedido)
- `--text-h`: `#3F403D` (texto principal) / `--text`: `#777873` (texto secundário)
- `--accent`: oliva pastel `#DDE2CF` (fundo de botão principal) / `--accent-contrast`: `#3F403D` (era branco — trocado para grafite, porque um fundo pastel claro exige texto escuro para contraste)
- `--accent-bg` / `--accent-border`: tons derivados do oliva, para realces e bordas suaves
- `--sage` / `--sage-text`: folha muito suave `#E1E9DE` / grafite-esverdeado `#4B5A47` (selo de anfitriã/participante, status "Ativa")
- **Tokens novos:** `--accent-rose` (rosé pastel `#F4E7E5`, usado nos selos de destaque do feed — Pergunta do Dia, Check-in, Comando da Comunidade) e `--danger`/`--danger-border`/`--danger-bg` (terracota suave `#B5615A`, substituindo o vermelho saturado `#d0244b` nos botões "Excluir")
- `--shadow`: suavizada de `rgba(91,46,63,0.12)` para `rgba(63,64,61,0.06)` — sombras bem mais discretas

**Achado e correção durante a implementação:** o token `--accent` era usado no app tanto como *fundo* (botões) quanto como *cor de texto* (rótulos de seção, links, avatares com iniciais, badge de participante). Ao virar um pastel claro, os usos como texto ficaram com contraste ruim (ex.: o título "ENGAJAMENTO" quase ilegível). Corrigido substituindo todo `color: var(--accent)` (texto) por `color: var(--sage-text)` (grafite-esverdeado escuro), mantendo `background: var(--accent)` intocado — validado depois via `getComputedStyle` no navegador, não só visualmente.
- `.community-badge` tinha `color: #fff` fixo sobre `background: var(--sage)` — corrigido para `color: var(--sage-text)`, pelo mesmo motivo (fundo virou claro, texto branco ficaria ilegível).

**Fora do escopo desta etapa, por decisão explícita:**
- Elementos decorativos botânicos (folhas, ramos, ilustrações lineares) **não foram adicionados** — a instrução os marcava como opcionais ("podem aparecer"), e adicionar novos assets/SVGs exigiria tocar em JSX de componentes, o que foi evitado para manter o escopo mínimo.
- **Dark mode não foi alterado** — o bloco `@media (prefers-color-scheme: dark)` em `index.css` continua com a paleta anterior (vinho escuro), intacto. Os tokens novos (`--accent-rose`, `--danger*`) não têm override específico no tema escuro e herdam o valor do tema claro se o navegador estiver em modo escuro — comportamento aceitável, não corrigido nesta etapa por estar fora do pedido.

**Verificação de contraste:** validada por leitura direta de `getComputedStyle` no navegador (não só inspeção visual) em: botão principal (`rgb(221,226,207)` fundo / `rgb(63,64,61)` texto), selo de post (`rgb(244,231,229)` / `rgb(63,64,61)`), botão excluir (`rgb(181,97,90)` sobre fundo transparente), aba ativa do painel e selo de comunidade — todos com contraste adequado.

**Testado ao vivo:** Professional (Marluce) — feed, Perguntas, Desafios, Círculos, Check-ins, Comandos, as 3 abas do `ProfessionalPanel` (incluindo as métricas); Member — feed e participação, sem nenhum elemento administrativo; Master — visão somente-leitura, sem regressão. Tela de login e recuperação de senha também verificadas (logo e nome "Círcula" preservados exatamente como estavam). Console sem erros de aplicação nos três papéis (só ruído de extensão do Chrome, já registrado em etapas anteriores como não relacionado ao app). `npm run build` passou.

**Limitação registrada:** responsividade 320–1440px não verificada visualmente nesta etapa (mesma limitação de ambiente das duas etapas anteriores da FASE 4) — nenhuma largura fixa foi introduzida, e toda a mudança foi só de cor/sombra/token, então o risco de quebra é baixo, mas a confirmação visual real fica pendente.

## Etapa concluída: FASE 4 — Painel da Professional (item 1: estrutura + organização do conteúdo)

- `ProfessionalPanel` criado: container com navegação por abas (Conteúdo / Comunidade / Assinaturas), controlada por estado local (sem router, sem mudança de URL).
- Aba **Conteúdo**: reúne `QuestionBankManager`, `ChallengeManager`, `CircleManager`, `CheckinManager` e `EngagementCommandManager` — os 5 já existentes das FASE 3, movidos para dentro do painel sem nenhuma alteração de lógica interna (mesmos hooks, mesmas props, mesmo comportamento).
- Aba **Comunidade**: só a estrutura (placeholder "Métricas da comunidade em breve.") — nenhuma métrica implementada nesta etapa, conforme combinado.
- Aba **Assinaturas**: placeholder visual puro ("Assinaturas em breve.") — sem nenhuma lógica de pagamento.
- `Dashboard.tsx` alterado somente no branch da Professional: `Feed`, `AddMemberForm` e `MemberList` continuam fora do painel, na área principal da comunidade, exatamente como antes.
- **Branch do Master e do Member em `Dashboard.tsx` não foram tocados** — Master continua vendo os 5 componentes como cards separados, sem abas, exatamente como era antes desta etapa; Member continua sem nenhum acesso a conteúdo administrativo.
- Nenhuma tabela, função, RLS, policy ou Auth foi alterada — só reorganização de componentes React já existentes e CSS novo (`.professional-panel`, `.panel-tabs`, `.panel-tab`, `.panel-tab--active`, `.panel-tab-content`, `.panel-placeholder`), reaproveitando as variáveis de cor já existentes (`--border`, `--text`, `--text-h`, `--accent`, `--accent-contrast`) — nenhuma cor nova foi introduzida.
- Testado ao vivo com Professional (Marluce): as 3 abas alternam corretamente; dentro de "Conteúdo", os 5 gerenciadores renderizam e continuam funcionando (confirmado visualmente: perguntas, desafios, círculos, check-ins e comandos todos presentes e com os mesmos controles de sempre).
- Testado ao vivo com Member: o painel não aparece (como esperado — Member nunca teve acesso a esses gerenciadores); resto da experiência (feed, desafios, círculos, check-ins como participante) sem nenhuma mudança visível.
- Testado ao vivo com Master: a visão antiga (cards separados, sem abas) permanece exatamente igual, confirmando que o branch do Master não sofreu nenhuma regressão.
- Console sem erros de aplicação nos três papéis (só ruído de extensão do navegador, não relacionado ao app, presente antes mesmo desta etapa).
- `npm run build` passou antes e depois dos testes no navegador.

**Limitação registrada:** a verificação de responsividade de 320px a 1440px não foi feita nesta etapa — o `ProfessionalPanel` reaproveita `.community-card` e os componentes internos já validados responsivos em etapas anteriores, e a única regra nova (`.panel-tabs` com `flex-wrap: wrap`) foi escrita já pensando em telas estreitas, mas isso ainda não foi confirmado visualmente. Fica pendente para quando o ambiente permitir redimensionamento real de janela (mesma limitação técnica já registrada na etapa de Comandos de Engajamento).

## Etapa concluída: FASE 4 — Métricas da Comunidade

- Função `community_metrics(p_community_id uuid, p_period_days integer default 30)` criada (`security definer`, `search_path` fixado em `public`), autorizada só para `is_master()` ou `owns_community(p_community_id)` — Member que tentar chamar recebe `not_authorized`. Verificada por execução real na aba "Comunidade" do painel, sem probe de bypass de API desta vez (a autorização segue o mesmo padrão de toda função já existente no projeto, já validado em fases anteriores).
- Antes de escrever a função, o schema existente foi checado por leitura (`information_schema.columns` de `post_reactions`, com o próprio usuário rodando a consulta) para confirmar a existência de `post_reactions.created_at` antes de usá-la — nenhuma coluna foi presumida.
- **Achado durante o desenho:** `circle_members.leaveCircle()` faz `DELETE` físico da linha, sem `left_at` nem histórico de saída. Por isso, "saída de círculo" **não entra** no cálculo de atividade — só "entrada em círculo" (`circle_members.joined_at`) é considerada, conforme decidido explicitamente para não alterar o comportamento já existente de "Sair" da FASE 3 e para não inventar um dado que não existe.
- Regra de Member ativa/inativa implementada com janela **fixa de 30 dias**, independente do seletor de período da tela: ativa = pelo menos uma ação (post, comentário, reação, resposta de check-in, progresso de desafio, entrada em círculo) nos últimos 30 dias. Não bloqueia, não remove e não altera acesso — é só uma métrica de leitura.
- Métricas de engajamento (posts, comentários, reações, progresso/conclusões de desafio, respostas de check-in, entradas em círculo) e "novas Members" respeitam o período selecionado (Hoje / 7 dias / 30 dias); "Total/Ativas/Inativas" seguem a regra fixa de 30 dias descrita acima, sempre reforçado por um texto explicativo na tela.
- Nenhuma tabela nova foi criada — todos os números vêm de agregação em tempo real sobre `community_members`, `posts`, `post_comments`, `post_reactions`, `challenge_progress`, `checkin_responses` e `circle_members`, todas já existentes.
- `src/types/communityMetrics.ts`, `src/hooks/useCommunityMetrics.ts` e `src/components/CommunityMetricsPanel.tsx` implementados, seguindo o mesmo padrão dos demais hooks/componentes do projeto.
- Aba "Comunidade" do `ProfessionalPanel` substituída: de placeholder para o painel real, com 4 blocos no topo (Total de Members / Ativas / Inativas / Novas), um seletor de período (reaproveitando a classe `.panel-tab` já existente das abas) e 6 blocos de engajamento (Posts / Comentários / Reações / Desafios / Check-ins / Círculos). Sem gráficos — só números, conforme combinado.
- CSS novo restrito a `.metrics-panel`, `.metrics-stats`, `.metric-tile`, `.metric-tile-value`, `.metric-tile-label`, `.metrics-hint`, `.metrics-section-title` e `.metrics-period`, reaproveitando variáveis de cor já existentes (`--social-bg`, `--text-h`, `--text`, `--accent`) — nenhuma cor nova.
- Master **não** recebeu nenhuma UI nova nesta etapa (conforme combinado) — a função autoriza `is_master()` pela mesma consistência de padrão de toda função administrativa do projeto, mas nenhum componente novo foi adicionado ao branch do Master em `Dashboard.tsx`.
- Testado ao vivo com Professional (Marluce): os 4 números do topo carregam corretos (2 membros, 2 ativas, 0 inativas, 2 novas em 30 dias); alternar entre Hoje/7 dias/30 dias atualiza só a seção de engajamento, mantendo Ativas/Inativas fixas; dados recarregam corretamente após `reload` da página.
- Testado ao vivo com Member: nenhum painel/métrica administrativa aparece em nenhum ponto da tela.
- Testado ao vivo com Master: nenhuma regressão — a comunidade termina exatamente onde terminava antes (Comandos de engajamento → Participantes), sem nenhum elemento novo.
- Console sem erros de aplicação nos três papéis (só ruído de extensão do Chrome, já registrado em etapas anteriores como não relacionado ao app).
- `npm run build` passou.

**Limitações registradas:** (1) responsividade 320–1440px não verificada visualmente nesta etapa — mesma limitação de ambiente já registrada nas duas etapas anteriores da FASE 4; os blocos de métrica usam `flex-wrap: wrap` pensando em telas estreitas, mas isso não foi confirmado. (2) Não foi feito um probe de API para confirmar por bypass de interface que Member recebe `not_authorized` ao chamar `community_metrics` diretamente — a autorização foi validada por leitura de código (mesmo padrão de `is_master()`/`owns_community()` já comprovado em todas as funções anteriores do projeto), não por execução real como Member. (3) Com apenas 2 membros reais na única comunidade existente, o cenário de "Inativas > 0" não pôde ser observado ao vivo — a lógica foi revisada, mas o valor real só será visto quando existir uma Member sem atividade há mais de 30 dias.

## Etapa em andamento: FASE 4 — Billing / Assinaturas (Asaas)

**Status:** camada implementada no código, ainda **não commitada** e ainda **não validada de ponta a ponta** (Edge Functions não deployadas, secrets Asaas não configuradas). Nenhuma cobrança real — o `_shared/asaas.ts` bloqueia qualquer chamada cuja chave não seja de Sandbox (`$aact_hmlg_`).

**Planos (`billing_plans`):**
- `platform` — `professional_monthly` R$ 59,90/mês · `professional_semiannual` R$ 299,90/sem · `professional_annual` R$ 499,90/ano.
- `community` — **`member_monthly` R$ 14,90/mês (1490 centavos) — preço definitivo** · `member_semiannual` R$ 74,90/sem · `member_annual` R$ 119,90/ano.
- As linhas vivem em `supabase/seed.sql` (não aplicado por `db push`) e no remoto. O `seed.sql` usa `on conflict (code) do nothing`, então **não** corrige preço de linha já existente. Para reconciliar o valor de `member_monthly` no remoto existe a migration idempotente `20260828120000_fix_member_monthly_price.sql` (`update ... where code = 'member_monthly' and price_cents <> 1490`), que não insere plano nenhum e é no-op se o valor já estiver correto.

**Fluxo de cobrança (origem única do valor):** o preço **nunca** é hard-coded no código — tanto o valor exibido na UI quanto o `value` enviado à Asaas derivam de `billing_plans.price_cents` em tempo de execução (`useBillingPlans` → `SubscriptionPanel`; `asaas-create-subscription` lê o plano por `code` e envia `price_cents / 100` + `billing_cycle`). Corrigir o preço = corrigir a linha em `billing_plans`.

**Implementado:**
- Trigger `create_community_trial` (baseline): ao entrar numa comunidade, cria `subscriptions` `subject='community'`, `status='trial'`, 21 dias, `plan_id` = `member_monthly`.
- Migration `20260825221926_billing_customer_data.sql`: CPF/CNPJ do cliente, com RLS.
- Edge Functions: `asaas-create-subscription`, `asaas-cancel-subscription`, `asaas-webhook` (idempotência via `webhook_events`, grava `payment_charges`, reativa/`past_due` conforme evento), `billing-daily-sweep` (bloqueia trial/past_due/cancelado vencidos + notificações D-3/D-2/D-1/D-0). `config.toml`: `[functions.asaas-webhook] verify_jwt = false`.
- Frontend: `types/billing.ts`, `hooks/useBillingPlans.ts`, `hooks/useSubscription.ts`, `hooks/usePlatformAccessBlocked.ts`, `hooks/useCommunityAccessBlocked.ts`, `components/SubscriptionPanel.tsx`, `AccessBlockedScreen.tsx`, `CommunityAccessBlockedCard.tsx`, `CommunitySubscriptionCard.tsx`, `MemberCommunityCard.tsx`; ligados em `App.tsx`, `Dashboard.tsx` e `ProfessionalPanel.tsx` (aba "Assinaturas" deixou de ser placeholder). O preço na UI é formatado em pt-BR via `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })` → "R$ 14,90".

**Pendente:** commit da camada; deploy das Edge Functions; configuração das secrets Asaas (Sandbox) e do `ASAAS_WEBHOOK_TOKEN`; aplicar a migration de preço no remoto (`db push`); validação de ponta a ponta do checkout, do webhook e do bloqueio pós-trial.

---

# ETAPA "PLATAFORMA COMPLETA" (iniciada 2026-08-31)

Objetivo desta etapa: implementar todas as funcionalidades estruturais que ainda faltam para o Círcula ser uma comunidade viva (eventos, biblioteca, mensagens, notificações, perfil rico, conexões, salvos, busca, convites, presença, store, billing e2e) **antes** do redesign visual das referências `1.jpeg`/`2.jpeg` (segunda etapa, separada).

Ordem definitiva: Eventos → Biblioteca → Perfil rico → Notificações → Mensagens → Salvos → Conexões → Busca global → Convites por link → Presença → Store aprimorada → Billing e2e → Professional (incremental) → Master → Home integrada.

Regras: RLS desde o início; sem dados fictícios; sem IA; não quebrar Feed/checkout/billing/auth/nav/Master; sem dependência nova sem justificativa; `tsc --noEmit` + `npm run build` após cada módulo; migration → `db push` → `migration list --linked` (local == remote). Sem git push / deploy / commit automático nesta etapa.

## Módulo 1 — EVENTOS ✅ (2026-08-31)

- Migration `20260831120000_community_events.sql` aplicada ao remoto (`db push` ok, `migration list --linked` = 12 local == 12 remote).
- Tabelas novas: `community_events` (id, community_id, circle_id nullable, created_by, title, description, cover_image_url, starts_at, ends_at nullable, is_online, location, online_url, capacity nullable, status draft/published/cancelled, timestamps) e `event_participants` (event_id, profile_id, joined_at, unique(event_id, profile_id) — cancelar = DELETE, mesmo padrão de `circle_members`). Nenhuma tabela existente alterada.
- Funções `SECURITY DEFINER` (molde de `can_view_challenge`): `can_view_event`, `can_participate_in_event` (exige `status='published'`), `event_participant_count`.
- RLS: select de evento = master OU dona OU (membro E status<>draft); insert/update/delete = `owns_community`. `event_participants`: select = `can_view_event`; insert = própria pessoa + `can_participate_in_event` + vaga livre (`capacity is null OR count < capacity`, checado na policy); delete = própria pessoa OU dona da comunidade. GRANT DML explícito para `authenticated`.
- Frontend: `types/event.ts`, `hooks/useEvents.ts` (molde de `useCircles`; `useEvents(null)` não faz fetch), `lib/formatEventDate.ts` (Intl nativo, sem dependência nova), `components/EventCard.tsx` (capa, data pt-BR, local/online, contagem + vagas, lista de confirmadas, botão RSVP toggle), `components/EventList.tsx` (destino para todos os papéis; filtros Próximos / Meus eventos / Encerrados), `components/EventManager.tsx` (formulário completo de criar/editar/cancelar/excluir para a anfitriã; `canManage={false}` = visão leitura do Master).
- Navegação: `NavKey` ganhou `'eventos'`; item de menu novo (só sidebar, `inBottomNav: false`) para member e professional-com-comunidade; `CalendarIcon` novo em `icons.tsx`. Destino "Eventos" reaproveita a mesma resolução de comunidade dos Círculos (picker quando multi-comunidade). `EventManager` entrou na aba "Conteúdo" do `ProfessionalPanel` e no branch do Master (leitura). Feed/checkout/billing/auth intocados.
- CSS novo restrito a classes `.event-*` no fim de `App.css`, só tokens já existentes (`--surface`, `--border`, `--olive*`, `--rose-100`, `--space-*`, `--shadow-sm`, `--radius-*`) — nenhuma cor nova.
- `npx tsc --noEmit` limpo; `npm run build` passou.

**Pendências / limitações:** ainda não testado ao vivo no navegador (só compilado); sem upload direto de imagem de capa (usa URL, mesmo padrão de círculos/produtos hoje); notificação de RSVP virá no módulo Notificações; isolamento entre duas comunidades não testável ao vivo (só existe uma comunidade real) — mesma limitação de todas as fases.

## Módulo 2 — CONTEÚDOS / BIBLIOTECA ✅ (2026-08-31)

- Migration `20260831130000_community_content.sql` aplicada (13 migrations local == remote).
- **Decisão de arquitetura:** tabela nova `community_content`, SEPARADA de `products`. Biblioteca = conteúdo gratuito curado pela anfitriã; `products` = catálogo pago com checkout/split/entitlements. Não misturar para não mexer no fluxo de pagamento já validado. Nenhuma tabela existente alterada.
- Tabelas: `community_content` (type recipe/article/tip/material/video/educational, title, summary, body, cover_image_url, external_url, category texto livre p/ filtro, circle_id nullable, status draft/published/archived) e `content_likes` (curtida toggle por DELETE, unique(content_id, profile_id)).
- Funções `SECURITY DEFINER`: `can_view_content`, `content_like_count`.
- RLS: select = master OU dona OU (membro E status='published'); insert/update/delete = `owns_community`. `content_likes`: select = `can_view_content`; insert = própria pessoa + `can_view_content`; delete = própria pessoa. GRANT DML explícito.
- Frontend: `types/content.ts` (com `CONTENT_TYPE_LABEL`), `hooks/useContent.ts` (molde de `useEvents`), `components/ContentCard.tsx` (capa, selo de tipo, categoria, resumo, "Ler"/"Abrir", curtir toggle), `components/ContentLibrary.tsx` (destino de todos; busca por texto — título/resumo/corpo/categoria — + filtro por tipo, sem IA, Seção 14 da spec), `components/ContentManager.tsx` (CRUD completo p/ anfitriã; `canManage={false}` = leitura Master).
- Navegação: `NavKey` ganhou `'biblioteca'`; item novo na sidebar (`inBottomNav: false`); `BookIcon` novo. Destino reaproveita a resolução de comunidade dos Círculos/Eventos. `ContentManager` na aba Conteúdo do painel e no branch Master.
- CSS `.content-*` no fim de `App.css`, só tokens existentes.
- `npx tsc --noEmit` limpo; `npm run build` passou.

**Pendências:** não testado no navegador; "salvar" conteúdo virá no módulo 6 (Salvos, `saved_items` polimórfico); sem upload direto de imagem (URL, como hoje).

## Módulo 3 — PERFIL RICO ✅ (2026-08-31)

- Migration `20260831140000_profile_rich.sql` aplicada (14 migrations local == remote).
- **Risco avaliado e considerado seguro:** `ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS bio text` + `city text`. A trigger `profiles_prevent_role_escalation` só reverte alterações da coluna `role` — ADD COLUMN nullable sem default não a aciona e não altera nenhuma linha existente (todas ficam NULL). `profiles` já tem `grant select, update to authenticated` + policy `profiles_update` (id = auth.uid() OR is_master()); o `updateProfile` já faz `.update(input)` genérico, então os campos entram sem policy/grant novo.
- RPC `profile_overview(p_profile_id uuid) returns jsonb` (`SECURITY DEFINER`, `search_path=public`): retorna `null` se quem chama não poderia ver o perfil pela predicada de `profiles_select`; senão retorna contagens (posts, comentários, círculos, desafios, conteúdos, eventos) **restritas às comunidades que quem chama também pode ver** (`is_master` / `owns_community` / `is_community_member`) — não vaza atividade de comunidade alheia. `grant execute ... to authenticated`.
- Frontend: `types/profile.ts` (Profile ganhou `bio`, `city`; novos `ProfileUpdateInput`, `ProfileOverview`), `useAuth.ts` (select estendido + `updateProfile(input: ProfileUpdateInput)`), `Dashboard.tsx` (assinatura de `onUpdateProfile` trocada para `ProfileUpdateInput`), `MyProfile.tsx` (campos Cidade + Sobre você na edição e na visualização), `ProfileCard.tsx` (mostra cidade, bio e um resumo de atividade real vindo de `profile_overview` — só itens com contagem > 0).
- CSS `.profile-city / .profile-bio / .profile-overview*` no fim de `App.css`.
- `npx tsc --noEmit` limpo; `npm run build` passou.

**Pendências:** não testado no navegador; conexões no `profile_overview` virão quando o módulo 7 existir (a RPC pode ser estendida depois); "publicações/desafios do próprio perfil" hoje são contagens, não listas navegáveis — pode evoluir na Home integrada.

## Módulo 4 — NOTIFICAÇÕES SOCIAIS ✅ (2026-08-31, código + build; teste no navegador pendente)

- **Decisão do usuário (sem reutilizar a tabela `notifications` de billing):** tabela NOVA `public.social_notifications`. A `notifications` de billing fica 100% intocada.
- Migration `20260831150000_social_notifications.sql` aplicada (15 migrations local == remote).
- Colunas: `id, profile_id (destinatária), actor_profile_id, type, title, body, related_post_id, related_comment_id, related_circle_id, related_event_id, related_challenge_id, read_at, created_at`. CHECK em `type` (post_comment / post_reaction / circle_join / event_rsvp / challenge_comment). `related_comment_id` sem FK (guarda `post_comments.id` OU `challenge_comments.id`); os demais `related_*` com FK `on delete cascade`.
- Índices: `(profile_id, created_at desc)`, `(profile_id, read_at)`, e um por `related_*` de navegação.
- RLS: SELECT e UPDATE só onde `profile_id = auth.uid()` (marcar `read_at`). **Sem policy/grant de INSERT ou DELETE para `authenticated`** — só os gatilhos `SECURITY DEFINER` (rodam como owner, ignoram RLS) e `service_role` escrevem. Nenhuma usuária consegue forjar notificação para outra.
- 5 gatilhos `AFTER INSERT`, `SECURITY DEFINER`, `search_path=public`, **cada corpo dentro de `BEGIN ... EXCEPTION WHEN OTHERS THEN NULL`** — se a notificação falhar, a operação principal (comentar/reagir/entrar/confirmar) segue normalmente:
  - `notify_on_post_comment` em `post_comments` → autora do post (`posts.author_id`), pula se autora == comentarista, grava `related_post_id` + `related_comment_id`.
  - `notify_on_post_reaction` em `post_reactions` → autora do post, pula si mesma, **dedup:** não recria se já existe notificação de reação NÃO LIDA da mesma actor para o mesmo post.
  - `notify_on_circle_join` em `circle_members` → `community_circles.created_by`, pula si mesma, `related_circle_id` + nome do círculo no `body`.
  - `notify_on_event_rsvp` em `event_participants` → `community_events.created_by`, `related_event_id` + título no `body`.
  - `notify_on_challenge_comment` em `challenge_comments` → `community_challenges.created_by`, `related_challenge_id` + `related_comment_id` + trecho no `body`.
- Frontend: `types/notification.ts`, `hooks/useSocialNotifications.ts` (busca da usuária logada, ordena por `created_at desc`, `unreadCount` real, `markRead`, `markAllRead` otimista + reconciliação, `refresh` estável para Realtime futuro), `components/NotificationList.tsx` (avatar real da actor, título, corpo, `formatRelativeTime`, estado lido/não lido, vazio = "Você está em dia por aqui 🌿"), `components/NotificationBell.tsx` (sino no header com badge só quando `unreadCount > 0`, número real, dropdown fecha em clique-fora/Esc, clique na notificação marca lida + navega por tipo → chave de navegação existente).
- Integração: `Dashboard.tsx` — 1 linha no header (`<NotificationBell>` antes do avatar, só para `role !== 'master'`) + import. `BellIcon` novo em `icons.tsx`. **`PrimaryNav`, `App.tsx`, Feed, `usePosts`, produtos, checkout, billing não foram tocados.** Master sem notificações sociais (coerente — Master não é membro de comunidade).
- CSS `.notification-*` no fim de `App.css`, só tokens existentes (`--surface`, `--border`, `--rose-50`, `--danger`, `--olive-bg`, `--shadow-lg`).
- `npx tsc --noEmit` exit 0; `npm run build` exit 0; nenhuma dependência nova.

**Validado:** migration aplicada transacionalmente (falharia em qualquer erro de SQL); `migration list --linked` local == remote (15/15); revisão estática dos 5 gatilhos contra o schema real; `tsc` + `build` limpos.
**Pendente (teste no navegador):** a extensão do Chrome não está conectada neste ambiente, então o fluxo ao vivo (login member + professional, comentar → conferir linha real em `social_notifications`, badge, marcar lida, clique navega) ainda **não foi exercitado**. Também pendente confirmar que o cache de schema do PostgREST reconhece o embed `actor:profiles!social_notifications_actor_profile_id_fkey` (se der 400 "could not find relationship", basta um reload de schema no projeto).

## Módulo 5 — MENSAGENS ✅ (2026-09-01, código + build + teste completo no navegador com 2 contas reais)

Conversas privadas 1:1 entre membros de uma mesma comunidade. Implementado junto com os demais módulos da etapa "Plataforma Completa"; fechamento documental e teste ao vivo feitos nesta rodada, sem alterar código ou banco.

**Componentes/hooks:**
- `hooks/useConversations.ts` — visão geral das conversas da usuária logada via RPC `conversations_overview()`; `totalUnread` (soma de `unread_count`); `startConversation(otherProfileId)` via RPC `get_or_create_direct_conversation`; assinatura Realtime em `messages` (INSERT) que revalida a lista; refetch também ao focar a janela. `useConversations(null)` não faz fetch (Master).
- `hooks/useConversation.ts` — thread de uma conversa: mensagens (`.order(created_at asc)`, limite 200, sem paginação infinita nesta etapa), `otherLastReadAt` (base do indicador "lida"), `markRead()` via RPC `mark_conversation_read`, `sendMessage(body)` (insert em `messages` + append local deduplicado por id), canal Realtime por conversa (`filter: conversation_id=eq.…`) que também dispara `markRead()` quando a mensagem chegada não é da própria usuária.
- `components/Messages.tsx` — orquestra as 3 vistas (`list`/`thread`/`new`); consome `initialConversationId` (deep-link vindo do `ProfileCard` ou do `NotificationBell`) e chama `onConsumedInitial()` para não reabrir em loop.
- `components/ConversationList.tsx` — lista com preview da última mensagem ("Você: …" quando é minha), horário relativo, badge de não lidas por conversa, botão "Nova conversa".
- `components/ConversationView.tsx` — thread com bolhas próprias/alheias, indicador "· lida" (quando `otherLastReadAt >= created_at` da minha última mensagem), marca como lida ao abrir e a cada nova mensagem, `MessageComposer` no rodapé.
- `components/MessageComposer.tsx` — textarea + Enter para enviar (Shift+Enter = nova linha).
- `components/NewConversation.tsx` — busca por nome (`ilike`, debounce 250ms) direto em `profiles`; a própria RLS de `profiles` já restringe a resultados de comunidade em comum, sem RPC de busca; exclui `role = 'master'` explicitamente.

**Tabelas e migrations** (ambas aplicadas e sincronizadas — 17 migrations local == remote):
- `20260831160000_messages_schema.sql`: tabelas `conversations` (`direct_key` único para dedupe 1:1), `conversation_participants` (`last_read_at`), `messages` (`body` 1–4000 chars); índices por `profile_id`/`conversation_id`/`(conversation_id, created_at desc)`/`last_message_at desc`.
- `20260831170000_messages_notifications_realtime.sql`: amplia `social_notifications` (Módulo 4) com o tipo `'direct_message'` + coluna `related_conversation_id` (alteração aditiva, CHECK só ganha um valor novo); gatilho `on_message_insert` (`AFTER INSERT` em `messages`, `SECURITY DEFINER`, à prova de exceção) atualiza `conversations.last_message_at` e cria a notificação para o outro participante; adiciona `messages` à publicação `supabase_realtime`.

**RPCs (`SECURITY DEFINER`, `search_path=public`):**
- `is_conversation_participant(uuid)` — helper anti-recursão (`stable`), usado pelas próprias policies.
- `get_or_create_direct_conversation(p_other uuid)` — exige `shares_active_community(p_other)`; dedupe via `direct_key` (`least`/`greatest` dos dois uuids) + `on conflict do update` idempotente.
- `mark_conversation_read(p_conversation_id, p_at default now())` — atualiza só a própria linha de `conversation_participants`.
- `conversations_overview()` — `table(...)`, `stable`, agrega por `auth.uid()`: conversa, outro participante, última mensagem, `unread_count` (mensagens do outro após meu `last_read_at`).

**RLS:** `conversations`/`conversation_participants`/`messages` com RLS habilitada; todas as policies de SELECT usam `is_conversation_participant()`; `conversation_participants` só permite UPDATE da própria linha (`profile_id = auth.uid()`); `messages` só permite INSERT com `sender_id = auth.uid()` e participação confirmada. **`authenticated` não tem INSERT em `conversations` nem em `conversation_participants`** — criar conversa é só via RPC `SECURITY DEFINER`. GRANT explícito de SELECT/UPDATE/INSERT nas tabelas e EXECUTE nas 4 funções.

**Navegação:** `NavKey 'mensagens'` (já existente), item no menu (sidebar + destino próprio, sem bottom-nav). Badge de não lidas no ícone da sidebar = `useConversations.totalUnread` (instância única no `Dashboard`, compartilhada com `NotificationBell`).

**Deep-link / início de conversa:** `ProfileCard` recebe `onStartConversation` (só quando `viewingProfileId !== profile.id`) → botão "Enviar mensagem" → `Dashboard.openConversationWith` → `startConversation` (RPC) → `setPendingConversationId` + `onNavigate('mensagens')` → `Messages` abre a thread direto. Mesmo mecanismo serve o clique numa notificação `direct_message` do `NotificationBell`.

**Teste completo realizado (2026-09-01), com as 2 contas reais já existentes — Professional "Nutri Marluce Bassani" (conta A) e Member "Usuario Teste" (conta B), ambas em Fluir & Florescer:**
- Abrir Mensagens pelo menu (A e B): lista carrega via `conversations_overview()`, sem erro.
- `ProfileCard` → "Enviar mensagem" (A abrindo o perfil de B em Participantes) → `get_or_create_direct_conversation` devolveu o **mesmo** `conversation_id` já existente (dedupe correto, não duplicou conversa) → navegou direto para a thread.
- Enviar (A) → aparece na hora, composer limpa, lista atualiza ("agora mesmo" + preview).
- Realtime: os 2 canais do app (`messages-overview-…`, `conversation-…`) ficam `joined`/SUBSCRIBED; uma assinatura independente (probe) confirmou o INSERT chegando pelo WebSocket com o `id`/`sender_id`/`body` corretos — pipeline banco → publicação `supabase_realtime` → RLS → cliente validado ponta a ponta. (Ressalva: com uma única sessão de navegador não foi possível observar a entrega simultânea em 2 clientes ao mesmo tempo — o teste alternou login entre as duas contas.)
- Badge (B, ao logar): `conversations_overview()` retornou `unread_count: 2`; badge **"2"** no ícone Mensagens da sidebar e na linha da conversa (texto em negrito).
- Abrir a conversa (B) → `mark_conversation_read` disparou (`conversation_participants.last_read_at` atualizado); `unread_count` → 0; badges (linha e sidebar) somem.
- Voltar à lista (B) → contador/badge continuam zerados.
- Responder (B) → nova bolha própria; lista/preview atualizam.
- Receber (A) → `conversations_overview()` mostra `unread_count: 1` da resposta de B; badge "1" na sidebar; abrir a conversa mostra a resposta e o indicador **"· lida"** na mensagem anterior de A (B já tinha lido); `mark_conversation_read` limpa o badge de A.
- Reload em cada etapa (A e B) → todas as mensagens persistem (vêm do banco), badges recalculam corretamente a cada carregamento.
- **RLS:** todas as 66 requisições Supabase capturadas no fluxo retornaram **200** (ou **204** nas chamadas de `mark_conversation_read`, RPC `void`) — nenhum 401/403. Policies e RPCs revisados contra o fluxo real: `conversations_overview`, `mark_conversation_read` e `get_or_create_direct_conversation` funcionaram exatamente como especificado, sem probe de bypass (não havia terceira conta disponível para testar negativamente).
- Console sem erro da aplicação nas duas contas.
- Responsividade: sem overflow no layout padrão testado; o teste sintético de contêiner a 320px **não é conclusivo** para `Messages` porque o layout de 2 painéis (`.messages-pane--list`/`.messages-pane--detail`, um por vez no mobile) depende de uma media query real (`@media (max-width: 860px)`) que o método de redimensionar só o contêiner não ativa — precisa de um teste com viewport real estreito para confirmar.

**tsc/build:** `npx tsc --noEmit` exit 0; `npm run build` exit 0.

**Pendências:** teste com viewport móvel real (320–860px) para confirmar o layout de painel único; teste negativo de RLS com uma terceira conta (confirmar que quem não participa da conversa não consegue ler `messages`/`conversation_participants` — hoje só verificado por leitura das policies, não por execução).

## Módulo 6 — FEED VIVO + HOME RICA ✅ (2026-09-01, código + build + testes no navegador Member/Professional/Master)

Objetivo: tornar o destino "Início" uma Home realmente rica em dados reais, sem redesign visual definitivo das referências (que continua sendo etapa posterior). Cinco sub-etapas — A1, A2, A4, E2 e um cleanup sem RPC — mais a avaliação de A5 (`home_today(uuid)`), que foi explicitamente **não** implementada.

**Regras respeitadas em todo o Módulo 6:** sem IA; sem dados fictícios; sem dependência nova; sem migration; sem RPC; sem `db push`; sem alterar banco/RLS; sem tocar `Feed.tsx`, `usePosts.ts`, billing, mensagens, notificações, produtos ou checkout; Master permanece exatamente como está (não monta a HomeToday); `tsc --noEmit` + `npm run build` após cada sub-etapa; nenhum `git add`/commit/push/deploy.

### A1 — Feed na navegação primária

- `NavKey 'feed'` e `FeedIcon` já existiam (base de navegação criada antes do A1); A1 apenas ativou o destino. Item "Feed" no menu para member e para professional-com-comunidade, presente também no **bottom-nav** do mobile.
- Destino `effectiveNav === 'feed'` no `Dashboard.tsx` (Opção A): renderiza `CommunityView` + `Feed`, com picker de comunidade quando a usuária participa de mais de uma. `canPost = role !== 'master'`. O FAB "+" do mobile leva ao Feed para member/professional; **Master mantém `Início`** (grade de comunidades). `CommunitySubscriptionCard` **não** foi reintroduzido; billing intocado.
- Isolamento comunidade × círculo preservado: `Feed` sem `circleId` → `usePosts` filtra `circle_id is null`; feed de círculo continua com `circle_id = id`.
- **Nada em `Feed.tsx` ou `usePosts.ts` foi alterado.** Sem migration, sem RPC.

### A2 — Upload direto de capas

- Novo `src/lib/uploadImage.ts` — `uploadCoverImage(file, uid)`: usa o bucket público **`avatars`** já existente (mesmo padrão de `useAuth.uploadAvatar` / `usePosts.uploadPostImage`), caminho `${uid}/covers/<timestamp>.<ext>` — a policy de INSERT do bucket já exige que a primeira pasta seja `auth.uid()`, então **nenhum bucket, policy ou regra de RLS nova**. Valida `image/*` e tamanho ≤ 5 MB; retorna `{ url, error }`.
- Novo `src/components/CoverImageInput.tsx` — seleção de arquivo (input `type=file` oculto + botão), preview (`blob:` local durante o envio → URL pública do Storage depois), botão "Remover", estado "Enviando…". Sem dependência nova. `onChange` devolve apenas uma string de URL, exatamente como o antigo `<input type="url">`.
- `<input type="url">` de capa substituído por `<CoverImageInput>` em `CircleManager` (formulários de criar e editar), `EventManager`, `ContentManager` e `CommunityView` (esta persiste na hora via `onSetCover`; nova prop `ownerId` para o caminho do upload). **Os hooks de persistência (`useCircles`, `useEvents`, `useContent`, `useCommunity`) não foram reescritos** — continuam recebendo `cover_image_url` como string.
- CSS `.cover-image-input*` no fim de `App.css`, só tokens já existentes; incluído `box-sizing: border-box` **escopado ao componente** (o projeto não tem reset global e é `content-box`) para o frame `width:100%` + borda não estourar o container em 320px.

### A4 — HomeToday v2 / Home rica

- Novo `src/components/HomeHighlights.tsx` — blocos ricos da Home, todos alimentados por hooks já existentes e renderizados com cards já existentes:
  - **Pergunta do dia**: publicação mais recente com `post_type = 'daily_question'` → `PostCard`.
  - **Comando da comunidade**: publicação mais recente com `post_type = 'engagement_command'` → `PostCard`.
  - **Check-in de hoje**: instância de check-in mais recente que o usuário logado ainda não respondeu → `CheckinResponseForm` (fluxo existente).
  - **Próximos eventos**: `useEvents`, ≤3 eventos futuros não-draft, ordenados por `starts_at` → `EventCard`; link "Ver todos" → `onNavigate('eventos')`.
  - **Destaques da biblioteca**: `useContent`, ≤3 conteúdos publicados → `ContentCard`; "Ver todos" → `onNavigate('biblioteca')`.
  - **Publicações recentes**: `usePosts`, ≤4 publicações do feed da comunidade, **excluindo** a pergunta/comando já exibidos → `PostCard`; "Ver todas" → `onNavigate('feed')`.
  - Cada bloco só aparece se houver **dado real**; se a fonte falhar, o hook devolve lista vazia e apenas aquele bloco some (tolerância por bloco). Sem IA, sem placeholder para "encher a tela".
- `HomeToday.tsx` — insere `<HomeHighlights>` logo após "Seu desafio". Blocos anteriores preservados: Saudação, Resumo do dia, Seu desafio, Atividade recente, Seus círculos, Atalhos.
- CSS: apenas `.home-card-stack` novo (pilha vertical de cards), só tokens existentes.
- Sem migration, sem RPC. `home_today(uuid)` deliberadamente adiado para avaliação (ver A5).

### E2 — Círculos sugeridos

- Bloco novo em `HomeToday.tsx`, logo após "Seus círculos": círculos da comunidade em foco nos quais o usuário logado **não** participa. Ordenação: **maior número de participantes** primeiro; desempate por **`created_at` mais recente**. Limitado a ~4. O bloco só aparece se houver ≥1 sugestão (sem placeholder vazio).
- Reaproveita `CircleCard` e o `joinCircle` da **mesma instância** de `useCircles` já usada pela HomeToday — **sem segundo mecanismo de entrada**. Ao entrar, o círculo sai das sugestões e passa para "Seus círculos" no próximo fetch (atualização reativa). "Ver todos" → `onNavigate('circulos')`.
- **Sem consulta nova**: deriva de `useCircles`, que já é restrita à comunidade em foco e à RLS — não revela círculos de outra comunidade. Sem migration, sem RPC, sem RLS nova.

### Cleanup sem RPC (opção A)

- `usePosts` passa a ter **instância única na Home**: criada em `HomeToday.tsx`, passada por prop `postsApi` para `HomeHighlights` (que deixou de instanciar seu próprio `usePosts`) **e** para `useHomeToday` como 4º parâmetro `feedPosts`.
- `useHomeToday.ts` — eliminadas **2 consultas**: (#1) `posts` para os ids das próprias publicações → agora derivados de `feedPosts`; (#2) `posts` novas nas últimas 24h → derivadas de `feedPosts`. **Mantidas**: #3 `post_comments` enriquecido nas minhas publicações (7d), #4 `post_reactions` idem (7d), e `community_metrics` (só professional). Chave estável `myPostIdsKey` (ids ordenados em string) evita re-disparar #3/#4 quando só a identidade do array de posts muda (ex.: após uma reação).
- **Consequência consciente e aprovada:** o "Resumo do dia" e a "Atividade recente" passam a considerar apenas o feed da comunidade (`circle_id is null`); interações em publicações minhas **dentro de círculos** não entram mais nesse resumo.
- **`usePosts.ts` e `Feed.tsx` não foram alterados** — o `usePosts` apenas passou a ser instanciado um nível acima. `App.tsx`, `PrimaryNav.tsx`, billing, mensagens, notificações, produtos, checkout, banco: intocados. Sem segunda fonte de verdade para posts (a instância é uma só).

### A5 — `home_today(uuid)` avaliado e **NÃO implementado**

- Inventário real das consultas no carregamento da Home (1 desafio ativo): **~18 round-trips (Member) / ~19 (Professional)** — 15 SELECTs REST + 3 RPCs (member) / 4 (professional) — já agrupados em ~5–6 ondas paralelas via `Promise.all`, sobre um dataset minúsculo (1 comunidade real, ~3 usuários, linhas de um dígito por tabela).
- Uma RPC `home_today(uuid)` `SECURITY DEFINER` poderia consolidar ~10–13 dessas consultas de leitura num único call, **mas não substituiria os hooks**: `usePosts`/`useEvents`/`useContent`/`useCheckins`/`useCircles`/`useChallenges` continuam montados porque cada ação de card precisa deles (reagir, comentar, RSVP, curtir, responder check-in, entrar/sair de círculo, participar/progresso de desafio) e do refetch reativo. A RPC rodaria **em adição**, criando uma segunda fonte de verdade (com necessidade de invalidação após cada ação) e uma superfície `SECURITY DEFINER` ampla sobre ~18 tabelas que teria de reimplementar à mão cada predicado de RLS (posts sem círculo, evento não-draft para member, conteúdo publicado, círculos da própria comunidade, etc.) — risco de vazamento cross-community/cross-círculo.
- **Decisão: A5 não implementada neste momento** porque a análise concluiu que **não há ganho suficiente para justificar a RPC** — as consultas já estão pequenas e paralelizadas, e o custo de segurança/manutenção supera o benefício de latência na escala atual. Alternativas mínimas sem RPC ficam registradas para o futuro, se necessário: (a) não buscar `community_checkins` na Home (é o banco administrativo do CheckinManager, sem uso ali); (b) opcionalmente fundir as 3 RPCs de contagem de desafio numa `challenge_summary(uuid)` enxuta — decisão à parte, **não** é `home_today`.

**Validado (2026-09-01, com a extensão do Chrome conectada):**
- `npx tsc --noEmit` **exit 0** e `npm run build` **exit 0** após cada sub-etapa (A1, A2, A4, E2, cleanup).
- Testes no Chrome com **Member** ("Usuario Teste"), **Professional** ("Nutri Marluce Bassani") e **Master**:
  - Home carrega para member e professional; todos os blocos aparecem e os números/conteúdos **conferem com os dados reais do Supabase** (pergunta do dia, comando, check-in pendente, próximos eventos, destaques da biblioteca, publicações recentes, resumo do dia, círculos sugeridos com a ordenação correta).
  - Ações dos cards funcionam com **atualização reativa** (reação, RSVP, curtida, resposta de check-in, entrada em círculo); o "Resumo do dia" não recalcula em mudança irrelevante (chave estável).
  - **Master continua sem HomeToday** — grade de comunidades intacta; `HomeHighlights` não monta.
  - **Feed continua funcionando** normalmente (instância própria de `usePosts` em `Feed.tsx`, não afetada).
  - Sem duplicação de post na Home.
- **RLS sem erros** e **Supabase sem 401/403 indevidos** para Member e Professional (todas as requisições do carregamento retornaram 200).
- **Responsividade validada**: sem barra de rolagem horizontal em **320px** nem em desktop (Home e Feed).
- **Console sem erro da aplicação** — apenas ruído de extensão do Chrome (origem `:0:0`, fora do código do app).
- **Nenhuma migration nova** em A1, A2, A4, E2 ou no cleanup — o bucket `avatars`, as políticas de Storage e todas as tabelas usadas já existiam. **Nenhuma RPC nova. Nenhum `db push`.**
- **Nenhum `git add`, commit, push ou deploy** em nenhuma sub-etapa.

**Pendências / observações:**
- Dados de teste criados no navegador durante a validação, aguardando remoção pela UI quando conveniente: círculos "Teste Capa A2", "Círculo Bem-estar (teste E2)" e "Círculo Movimento (teste E2)" (este com a anfitriã como membro); evento "Roda de conversa — Cuidar de si (teste A4)"; conteúdo "Guia de respiração consciente (teste A4)". Nada existente foi apagado.
- Cosmético (não corrigido, exige mudança de código): no bloco "Check-in de hoje", o texto da instância aparece acima do prompt fixo do `CheckinResponseForm` — parece duplicado quando os dois textos coincidem.
- Ponto de parada consciente do projeto: **funcionalidade primeiro, documentação alinhada, sem criar RPC só para reduzir consultas que já estão pequenas e paralelizadas.**

## Etapa concluída: Fase 6 — Desafios enriquecidos

Enriquece os Desafios (seções 6 e 7) sem trocar o mecanismo já validado na FASE 3: **cada dia continua sendo uma linha em `challenge_activities` com `content` próprio** (Dia 1 "beber 2 litros de água", Dia 2 "caminhar 30 minutos"…). O que a Fase 6 acrescenta: capa, período real com início/fim, edição das atividades pela Nutri, registro de conclusão por participante e **a estrutura (só as colunas) do sistema de pontos** — a pontuação em si é exclusivamente da Fase 7.

**Decisões aprovadas pelo usuário antes da implementação:**
1. `challenge_current_day` — `CREATE OR REPLACE` da função existente (não é RPC nova; escopo mínimo).
2. Campos de pontos criados já na Fase 6 (`completion_points`, `per_day_points`); a Nutri configura no ChallengeManager. **Nada é calculado, concedido, somado, exibido como saldo ou registrado em ledger.**
3. `challenge_completions` criada para registrar a conclusão, protegida contra duplicação.
4. `ends_on` criado e persistido — período real (`starts_on` + `ends_on`).
5. Área de Desafios permanece dentro da comunidade + bloco "Seu desafio" na Home; **sem novo item no menu lateral**.

### Migration — `supabase/migrations/20260903120000_challenge_rich.sql` (única; APLICADA)

`supabase db push` executado em 2026-09-02 (autorização separada). `supabase migration list --linked` → `20260903120000` `local == remote` (23 migrations em sincronia). 100% aditiva, transacional, idempotente, reversível. **Nenhuma linha existente é apagada.**

- `community_challenges` `+cover_image_url text` `+starts_on date not null` `+ends_on date` `+completion_points integer not null default 0` `+per_day_points integer not null default 0`.
  - Backfill: `starts_on = created_at::date` (created_at sempre no passado → `challenge_current_day` segue >= 1 para os desafios existentes, sem regressão); `ends_on = starts_on + (nº de dias − 1)`.
  - CHECKs: `ends_on is null or ends_on >= starts_on`; `completion_points >= 0`; `per_day_points >= 0`.
- `challenge_progress` `+points_awarded integer not null default 0` — reservado Fase 7.
- `challenge_completions` (NOVA): `id, challenge_id (fk cascade), profile_id (fk cascade), completed_at timestamptz default now(), points_awarded integer not null default 0`; `unique (challenge_id, profile_id)` (anti-duplicação); índices em `challenge_id` e `profile_id`. RLS habilitada + 3 policies + GRANT DML explícito para `authenticated`.
- `challenge_current_day(uuid)` — `CREATE OR REPLACE`, mesma assinatura/retorno/volatilidade/`search_path`. Base passa de `created_at` (janela de 24 h) para `starts_on` (dia de calendário). Piso `0` = "ainda não começou" (antes de `starts_on` nenhum dia é liberado; a policy `challenge_progress_insert` já exige `day_number <= challenge_current_day`, então marcar dia antes do início fica bloqueado no banco). Teto = nº de atividades (inalterado).

### RLS / policies

- Colunas novas em `community_challenges` / `challenge_progress`: **nenhuma policy nova** — as policies existentes decidem por `community_id` / `owns_community()` / `is_community_member()` / `is_master()` / `profile_id = auth.uid()`, nunca por coluna (mesma lógica de `recipe_fields`).
- `challenge_activities` (Nutri editando dias): policies de insert/update/delete por `owns_community()` **já existem** — nenhuma mudança.
- `challenge_completions` (tabela nova):
  - **select**: `profile_id = auth.uid()` OU (`owns_community(c.community_id)` OU `is_master()`) via `community_challenges` — espelha `challenge_progress_select`.
  - **insert**: `profile_id = auth.uid() and can_participate_in_challenge(challenge_id)` (reaproveita função existente — sem função nova).
  - **delete**: `profile_id = auth.uid()` (permite reverter a conclusão ao desmarcar um dia).
  - `grant select, insert, delete … to authenticated`.

### Arquivos

- **Criados:** `supabase/migrations/20260903120000_challenge_rich.sql`; `src/lib/challengePeriod.ts` (formatação pt-BR e aritmética de datas 'YYYY-MM-DD', sem dependência nova — no espírito de `lib/formatEventDate.ts`).
- **Alterados:** `src/types/challenge.ts` (campos novos, `ChallengeCompletion`, `ChallengeActivityDraft`, `ChallengeDetailsInput`); `src/hooks/useChallenges.ts` (`CHALLENGE_SELECT` + colunas; `createChallenge`/`updateChallenge` com capa/data/pontos; `updateChallenge` **passa a sincronizar `challenge_activities`** com guarda contra remover dia que já tem progresso; `myCompletions` + `refreshCompletions`); `src/hooks/useChallengeProgress.ts` (`duration`/`completedInitially`; `allDaysDone`; reconciliação automática de `challenge_completions`); `src/components/ChallengeManager.tsx` (capa, data de início, pontos, edição dos dias); `src/components/ChallengeCard.tsx` (capa, período, "Começa …", botão "Concluir o dia", selo "Desafio concluído"); `src/components/HomeToday.tsx` (props `isCompleted` / `onCompletionChange`); `src/App.css` (`.challenge-cover`, `.challenge-period`, `.challenge-notstarted`, `.challenge-points-row`, `.challenge-points-field`, `.challenge-field-hint`, `.challenge-complete-day` no azul `--help-accent` do rodapé do logo, `.challenge-completed-badge` — só tokens existentes).
- **Intocados:** `Feed.tsx`, `usePosts.ts`, Mensagens, Receitas, Humor diário, Momento de alegria, Pedido de ajuda, `challenge_comments`, `PrimaryNav.tsx`/`App.tsx` (sem rota nova), auth, billing, notificações e todas as outras migrations.

### Desafio de 1 dia vs. vários dias

- **1 dia:** 1 `challenge_activities` (`day_number = 1`), `starts_on = ends_on`. `challenge_current_day` → 0 antes, 1 no dia. Marcar o dia 1 conclui o desafio → linha em `challenge_completions`.
- **Vários dias:** N atividades (1..N), `ends_on = starts_on + (N−1)`. Um dia liberado por data de calendário. Concluir os N dias → `challenge_completions`.
- **Atividade diferente por dia:** já era o modelo — `challenge_activities.content` por `day_number`, `unique (challenge_id, day_number)`. Nenhuma coluna nova para isso.

### Fluxo da Nutri (ChallengeManager, canManage)

Criar: título, descrição, **capa** (`CoverImageInput` → `uploadCoverImage` → bucket `avatars`, path `${uid}/covers/…`; sem bucket/policy nova), **data de início** (`<input type="date">`, default hoje), **pontos** (2 campos numéricos, default 0, com aviso "a pontuação entra em vigor numa próxima etapa; por enquanto só ficam guardados"), lista de dias dinâmica (1 = desafio de 1 dia). `ends_on` calculado no cliente.
Editar: o formulário passa a semear os dias de `challenge.activities`; salvar sincroniza `community_challenges` + `challenge_activities` (atualiza texto, adiciona dia ao fim, remove último dia **apenas se não houver progresso** — senão erro pedindo para desativar em vez de encurtar).

### Fluxo da usuária (ChallengeCard, canParticipate)

Vê: **capa** no topo, **título**, **período** ("3 de set – 9 de set"), descrição, contagens coletivas. Se `starts_on` no futuro (`currentDay = 0`): "Começa amanhã / em 3 de setembro", trilha inativa, sem botão. Em andamento: "Dia X de Y" + atividade do dia + anel + trilha + **botão "Concluir o dia X"** (azul do rodapé do logo). Ao fechar o último dia: **selo "Desafio concluído 🎉"** e `challenge_completions` inserida; desmarcar um dia reverte a conclusão. **Nenhum ponto aparece para a usuária nesta fase.**

### Como os pontos ficam apenas preparados

`completion_points` e `per_day_points` (em `community_challenges`) e `points_awarded` (em `challenge_progress` e `challenge_completions`) são criados com `default 0`. Nenhum código de cliente ou de banco lê esses valores para calcular, conceder, somar, exibir saldo ou manter ledger. A Nutri apenas configura `completion_points` / `per_day_points`; tudo o mais é Fase 7.

### Anti-duplicação / conclusão indevida

- `unique (challenge_id, profile_id)` em `challenge_completions`; o cliente checa `myCompletions` antes de inserir e trata `duplicate key` como sucesso (mesmo padrão de `joinChallenge`).
- Conclusão só é gravada quando `completedDays.size >= duration`, e cada linha de `challenge_progress` só entra se o dia estiver liberado (`day_number <= challenge_current_day`) e for do próprio `auth.uid()`. A policy de insert de `challenge_completions` ainda exige `profile_id = auth.uid()` e `can_participate_in_challenge`.
- **Limitação registrada:** não há verificação server-side de "N dias ⇒ conclusão" (sem trigger nesta fase, por decisão de não criar RPC/trigger novos); a garantia forte fica para a Fase 7 junto com os pontos. Desafio antigo com todos os dias já fechados antes desta feature só grava a conclusão quando a participante desmarcar/remarcar um dia.

### tsc / build / lint

- `npx tsc --noEmit` → **exit 0**.
- `npx tsc -b` → **exit 0**.
- `npm run build` (`tsc -b && vite build`) → **exit 0** (aviso de chunk > 500 kB é pré-existente, não relacionado).
- `npx oxlint` → **0 erros**, 39 warnings (todos `react(set-state-in-effect)` pré-existentes em todo o projeto; os 2 novos em `useChallengeProgress.ts` seguem exatamente o mesmo padrão de `useChallenges.ts`, `useAuth.ts` etc.).

### Verificação de schema (catálogo do Postgres remoto, pós-`db push`)

Conferido por leitura do catálogo: as 5 colunas novas de `community_challenges` (tipos, `NOT NULL` só em `starts_on`, `default 0` em `completion_points`/`per_day_points`); os 3 CHECKs (`period_check`, `completion_points_check`, `per_day_points_check`); `challenge_progress.points_awarded` (`integer NOT NULL default 0`); `challenge_completions` completa — PK, FK `challenge_id`/`profile_id` `ON DELETE CASCADE`, **`UNIQUE (challenge_id, profile_id)`**, índices em `challenge_id` e `profile_id`, `rowsecurity = true`, as 3 policies exatas (select `profile_id = auth.uid() OR owns_community/is_master`; insert `profile_id = auth.uid() AND can_participate_in_challenge`; delete `profile_id = auth.uid()`), GRANT `SELECT, INSERT, DELETE` só para `authenticated` (`anon`/`service_role` sem DML); `challenge_current_day(uuid)` `STABLE SECURITY DEFINER`, `search_path = public`, corpo com base `starts_on` e piso `0`.

**Backfill dos dados existentes:** o único desafio real ("3 dias cuidando de mim (editado)", criado 2026-08-23, 3 dias) recebeu `starts_on = 2026-08-23` (`= created_at::date`), `ends_on = 2026-08-25` (`= starts_on + 2`), `completion_points = 0`, `per_day_points = 0`. `challenge_current_day` continua retornando **3** (comportamento idêntico ao anterior). **Nenhum dado existente foi alterado além desse backfill aditivo.**

### Testes funcionais e de RLS — executados por SQL (extensão do Chrome indisponível)

**O teste visual no Chrome ficou PENDENTE por indisponibilidade da extensão/ambiente** (mesma limitação registrada em todas as fases anteriores). Em substituição, os fluxos foram exercidos com SQL real contra o banco remoto **simulando cada usuária** (`set role authenticated` + `request.jwt.claims` com o `sub` de cada perfil de "Fluir & Florescer": Nutri Marluce `1c20d81a…`, Member "Usuario Teste" `94bc64f8…`, Master `18004064…`) — isso aciona exatamente as mesmas policies e predicados que o cliente do navegador usaria.

**Professional (Nutri):**
- Criou um **desafio de 1 dia** (capa, `starts_on = ends_on = hoje`, `completion_points = 50`, `per_day_points = 10`, 1 atividade) e um **desafio de vários dias** (capa, `ends_on = starts_on + 3`, `completion_points = 100`, `per_day_points = 15`, 4 atividades distintas: água / caminhar 30 min / fruta / meditar 10 min). INSERTs em `community_challenges` e `challenge_activities` passaram sob `owns_community`.
- **Persistência confirmada** por novo SELECT no formato do `CHALLENGE_SELECT`: capa, período, pontos e todas as atividades na ordem correta.
- **Edição confirmada**: renomear, `completion_points 100→120`, `per_day_points 15→20`, empurrar `starts_on` para amanhã (`ends_on` recalculado), trocar o texto do Dia 2, adicionar Dia 5 — todos os UPDATE/INSERT passaram sob `owns_community`. Após a edição, `starts_on = amanhã` → `challenge_current_day = 0` ("ainda não começou").
- **Guarda de remoção de dia**: no desafio com progresso, o precheck do `updateChallenge` (`select … from challenge_progress where day_number in (removidos) limit 1`) retornou linha → o cliente aborta e não apaga; no desafio sem progresso, retorna 0 → remoção permitida.
- CHECKs negativos: `ends_on < starts_on` → `23514 period_check`; `completion_points = -10` → `23514 completion_points_check`.

**Member ("Usuario Teste"):**
- Vê os desafios da comunidade com capa, período, nº de dias e `challenge_current_day`.
- Entrou no desafio de 1 dia, **marcou o dia liberado** (`challenge_progress`, `points_awarded = 0`) e **concluiu o desafio** (`challenge_completions` inserida, `points_awarded = 0`).
- **Anti-duplicação**: segundo INSERT de conclusão → `23505 duplicate key … challenge_completions_challenge_id_profile_id_key`.
- **Reverter conclusão**: ao desmarcar o dia, o cliente apaga a conclusão (`delete challenge_completions` sob `profile_id = auth.uid()`); as duas exclusões passaram e as contagens voltaram a zero.
- Tentou marcar o Dia 1 de um desafio **ainda não começado** → **bloqueado** (`42501` em `challenge_progress`, pois `day_number(1) <= challenge_current_day(0)` é falso).

**Master:**
- Branch permanece `main` (nenhuma operação git feita).
- Vê os desafios (`is_master()` no select) e vê `challenge_completions` de outra usuária (ramo `is_master` do select) — visão administrativa somente-leitura.
- Criar desafio → **bloqueado** (`42501` em `community_challenges`); editar desafio → **0 linhas**; marcar um dia → **bloqueado** (`42501`). Nenhuma permissão indevida.

**RLS — resultados:**
- Member **não administra**: criar/editar `community_challenges` e editar `challenge_activities` → todos bloqueados (`42501` ou 0 linhas); confirmado que `completion_points` não mudou após tentativa de UPDATE indevido.
- Professional administra **só a própria comunidade** — policies `owns_community(community_id)` em insert/update/delete (inalteradas pela migration).
- **Progresso e conclusão respeitam `auth.uid()`**: Member inserindo `challenge_progress` ou `challenge_completions` com `profile_id` de outra pessoa → `42501` nos dois casos. Member lendo `challenge_completions` → só a própria linha.
- **Teste negativo de isolamento entre comunidades: PENDENTE** — não existe uma segunda comunidade real autorizada para teste. As policies novas reutilizam `can_participate_in_challenge` / `owns_community` / `is_master` (mesmas funções já usadas nas fases 3–5); o teste negativo real cross-community fica para quando houver uma segunda comunidade.

**Network / Console / Responsividade 320–1440px: PENDENTES** — dependem de um ambiente com a extensão do Chrome. Em substituição ao Network, cada operação legítima retornou sucesso e cada operação indevida retornou o erro esperado (`42501` RLS, `23505` unique, `23514` check), sem `4xx` inesperado em caminho legítimo. O CSS novo usa apenas `flex-wrap`, unidades relativas e tokens existentes (risco de quebra baixo, mas sem confirmação visual).

### Dados de teste e estado final do banco

Os desafios de teste criados durante a validação ("F6 TESTE 1 dia" e "F6 TESTE varios dias (editado)") foram **removidos** pela Nutri (policy de delete `owns_community`); o cascade limpou atividades, participação, progresso e conclusões. **Permanecem apenas os dados pré-migração**: 1 desafio (o original "3 dias cuidando de mim (editado)"), 3 atividades, 2 participações e 3 progressos — todos filhos desse desafio, nada órfão, nada alterado além do backfill aditivo de `starts_on`/`ends_on`. 0 conclusões.

### Git

**Nenhum `git add`, `git commit`, `git push` ou deploy foi feito.** Todas as alterações permanecem apenas na árvore de trabalho. Arquivos da Fase 6 na árvore: `supabase/migrations/20260903120000_challenge_rich.sql` (novo), `src/lib/challengePeriod.ts` (novo), e modificações em `src/types/challenge.ts`, `src/hooks/useChallenges.ts`, `src/hooks/useChallengeProgress.ts`, `src/components/ChallengeManager.tsx`, `src/components/ChallengeCard.tsx`, `src/components/HomeToday.tsx`, `src/App.css` e `docs/ESPECIFICACAO_CIRCULA.md`.

### Pendências desta fase (para retomar depois)

1. Teste visual no Chrome (Professional/Member/Master): capa, período, "Começa em…", botão "Concluir o dia", selo "Desafio concluído", aba Network, console sem erro.
2. Responsividade 320 / 375 / 768 / 1024 / 1280 / 1440 px do card e do formulário.
3. Teste negativo de isolamento entre comunidades (precisa de uma segunda comunidade real autorizada).

## Etapa em revisão: Fase 7 — Sistema de pontos (código pronto, migration NÃO aplicada)

Sistema real de pontos **por comunidade**, com saldo, histórico append-only auditável e preparação para descontos futuros na loja. **Resgate NÃO faz parte desta fase** — nenhum checkout / produto / desconto é tocado.

**Decisões definitivas do usuário:**
1. Participação recorrente = **somente `daily_mood_entries`** (humor do dia). `checkin_responses` não é usado.
2. Valor via **`communities.recurring_points_per_day`** (Nutri configura na aba Pontos; default 0 = desligado).
3. **Sem estorno automático** — cada ação concede uma única vez; desmarcar um dia não retira pontos.
4. **Sem backfill** — o sistema começa a pontuar na implantação.
5. **Master nunca vê `point_ledger`/`point_accounts` individuais** — só agregados via `points_community_summary`.
6. Concessão manual **1–1000** por operação, só pela Nutri dona, nunca para si.
7. Migration: `supabase/migrations/20260904120000_points_system.sql`.
8. `PointsWidget` na HomeToday e no MemberCommunityCard, sempre no contexto da comunidade atual.
10. Desafios usam os campos da Fase 6 (`per_day_points`, `completion_points`) — Fase 6 não é alterada.

### Migration `20260904120000_points_system.sql` (única; NÃO aplicada)

100% aditiva, transacional (`begin/commit`), idempotente (`create table/function/trigger if not exists` / `create or replace` / `drop … if exists` + `create`), reversível. Nenhuma linha existente é alterada. Modelada sobre `create_product_order` (insert idempotente + `on conflict`), `community_metrics` (agregado + `raise 'not_authorized'`) e os 6 gatilhos `notify_on_*` (AFTER INSERT SECURITY DEFINER à prova de exceção).

**Tabelas / colunas:**
- `communities` `+ recurring_points_per_day integer not null default 0` + CHECK `between 0 and 1000`.
- `point_accounts` (NOVA) — saldo cache O(1): `id, community_id (fk cascade), profile_id (fk cascade), balance integer not null default 0, updated_at`. `unique (community_id, profile_id)`; CHECK `balance >= 0`; índices `(profile_id)` e `(community_id, balance desc)`.
- `point_ledger` (NOVA) — histórico append-only: `id, community_id (fk cascade), profile_id (fk cascade), amount integer not null CHECK > 0, reason text CHECK in (challenge_day | challenge_completion | recurring_participation | manual), source_type text, source_id uuid, dedupe_key text not null UNIQUE, note text, awarded_by uuid (fk set null), created_at`. Índices `(community_id, profile_id, created_at desc)` e `(profile_id, created_at desc)`. **Sem coluna `updated_at`.**

**Funções (todas `SECURITY DEFINER`, `SET search_path TO 'public'`, sem SQL dinâmico):**
- `_award_points(community_id, profile_id, amount, reason, source_type, source_id, dedupe_key, note, awarded_by) returns uuid` — **interna**, `revoke all … from public`. `insert into point_ledger … on conflict (dedupe_key) do nothing returning id`; se nada retornou, reconsulta por `dedupe_key` e devolve o lançamento existente (no-op, sem crédito). Não mexe em `point_accounts`.
- `award_points_manual(community_id, profile_id, amount, note default null) returns uuid` — **RPC pública** (`grant execute to authenticated`). Guardas: `owns_community` senão `not_authorized`; `profile_id = auth.uid()` → `cannot_grant_to_self`; alvo tem de ser `community_members.status='active'` senão `target_not_member`; `amount 1..1000` senão `amount_out_of_range`. Chama `_award_points(reason='manual', dedupe_key='manual:'||gen_random_uuid(), awarded_by=auth.uid())` — concessões manuais nunca são deduplicadas entre si.
- `points_community_summary(community_id, period_days default 30) returns jsonb` — RPC (`grant execute to authenticated`), `stable`. Guard `is_master() OR owns_community` senão `not_authorized`. Retorna `total_points_all_time`, `total_points_period`, `period_days`, `by_reason` (jsonb), `earners_count`, `top_earners`. **`top_earners` (lista nominal) só quando `owns_community`; para o Master vem `null`.**
- `_points_apply_balance()` — trigger fn (item abaixo).

**Triggers (`AFTER INSERT … FOR EACH ROW`, `SECURITY DEFINER`):**
- `trg_points_apply_balance` em `point_ledger` → `insert into point_accounts (…, balance) values (…, new.amount) on conflict (community_id, profile_id) do update set balance = point_accounts.balance + excluded.balance, updated_at = now()`. `on conflict do update` toma trava de linha → concessões concorrentes à mesma conta serializam, sem lost update. Único lugar que mantém o saldo.
- `trg_points_on_challenge_day` em `challenge_progress` → lê `community_challenges.per_day_points`; se `> 0`, `_award_points(reason='challenge_day', source='challenge_progress', dedupe_key='cday:<challenge>:<profile>:<day_number>')`. Corpo em `BEGIN … EXCEPTION WHEN OTHERS THEN NULL` — nunca bloqueia a conclusão do dia da Fase 6.
- `trg_points_on_challenge_completion` em `challenge_completions` → `completion_points`; `dedupe_key='cdone:<challenge>:<profile>'`. Idem exception-safe.
- `trg_points_on_recurring_participation` em `daily_mood_entries` → `communities.recurring_points_per_day`; `dedupe_key='recur:<community>:<profile>:<entry_date>'`. `AFTER INSERT` só dispara na criação real (o `upsert` do cliente que *atualiza* o humor dispara UPDATE, sem trigger). Idem exception-safe.

**RLS / GRANT:**
- `point_accounts` / `point_ledger`: `enable row level security`. **Só policy de SELECT**: `profile_id = auth.uid() OR owns_community(community_id)` — **sem `is_master()`**. **Nenhuma policy de INSERT/UPDATE/DELETE.** GRANT só `SELECT` para `authenticated` (nada de INSERT/UPDATE/DELETE). Os únicos escritores são as funções `SECURITY DEFINER` acima (rodam como owner das tabelas → RLS bypassada, mesmo padrão de `social_notifications`). `point_ledger` é, portanto, **append-only por RLS, não só por convenção**.
- `communities_update` já permite `owner_id = auth.uid()` → a Nutri configura `recurring_points_per_day` direto, sem RPC nova.

### Idempotência e concorrência

`point_ledger.dedupe_key UNIQUE` é a **única** garantia. As chaves são determinísticas por ação real (`cday:…`, `cdone:…`, `recur:…`) → desmarcar+remarcar um dia, refazer o upsert de humor, duplo disparo concorrente e replay colidem todos na mesma chave → `on conflict (dedupe_key) do nothing` + fallback select → **nenhum segundo crédito**. Concessão manual usa `manual:<uuid>` (ato deliberado, sem dedupe). Concorrência de saldo: `on conflict (community_id, profile_id) do update set balance = balance + excluded.balance` (trava de linha).

### Isolamento por comunidade

Todo lançamento e toda conta carregam `community_id`, **sempre derivado no servidor** da linha de origem (`community_challenges.community_id` via join; `daily_mood_entries.community_id` direto) — nunca de input do cliente no caminho de gatilho. `award_points_manual` recebe `p_community_id` mas o gateia com `owns_community(p_community_id)`. `(community_id, profile_id)` é a identidade da conta; as policies (`owns_community(community_id)`) + `.eq('community_id', …)` nos hooks impedem qualquer cruzamento de saldo entre comunidades.

### Segurança da SECURITY DEFINER

`search_path` fixo em `'public'` em toda função; `_award_points` sem GRANT público (`revoke all … from public`); `award_points_manual` revalida `owns_community` + `<> auth.uid()` + membership + faixa a cada chamada; `points_community_summary` revalida `is_master() OR owns_community`; sem SQL dinâmico; todos os parâmetros são tipados (uuid/int/text) usados só como valores; `auth.uid()` validado nas guardas. Member não tem nenhum caminho de escrita em `point_*`; a Nutri não concede para si.

### Fontes de pontos (nenhuma outra)

| Fonte | Gatilho | Valor | dedupe_key |
|---|---|---|---|
| Conclusão de um dia de desafio | `challenge_progress` AFTER INSERT | `community_challenges.per_day_points` | `cday:<challenge_id>:<profile_id>:<day_number>` |
| Conclusão de um desafio | `challenge_completions` AFTER INSERT | `community_challenges.completion_points` | `cdone:<challenge_id>:<profile_id>` |
| Participação recorrente | `daily_mood_entries` AFTER INSERT | `communities.recurring_points_per_day` | `recur:<community_id>:<profile_id>:<entry_date>` |
| Concessão manual | RPC `award_points_manual` | 1–1000 | `manual:<uuid>` |

### Arquivos

- **Criados:** `supabase/migrations/20260904120000_points_system.sql`; `src/types/points.ts`; `src/hooks/usePoints.ts` (`usePoints` — saldo/extrato da própria usuária na comunidade; `usePointsAdmin` — resumo, saldo por participante, extrato da comunidade, `awardManual`, `setRecurringConfig`); `src/components/PointsWidget.tsx`; `src/components/PointsHistory.tsx` (origem, descrição, valor, data, comunidade; opção `showWho` para o extrato da Nutri); `src/components/PointsPanel.tsx` (aba Pontos da Nutri).
- **Alterados:** `src/components/ProfessionalPanel.tsx` (`PanelTab` += `'pontos'`, botão de aba, `<PointsPanel>`); `src/components/HomeToday.tsx` (`<PointsWidget>` após "Seu desafio"); `src/components/MemberCommunityCard.tsx` (`<PointsWidget>` após o `ChallengeManager`); `src/App.css` (bloco `.points-*` — só tokens existentes; botões no azul `--help-accent` do rodapé do logo; cards brancos, borda fina, sombra suave); `docs/ESPECIFICACAO_CIRCULA.md`.
- **Intocados:** `Feed.tsx`, `usePosts.ts`, Mensagens, Receitas, **Humor diário (client — o gatilho é 100% no banco)**, Momento de alegria, Pedido de ajuda, checkout/billing/produtos (`SubscriptionPanel`, `useProductCheckout`, `create_product_order`, `product_*`), `PrimaryNav.tsx`/`App.tsx` (sem rota nova), lógica da Fase 6 (`useChallenges`/`useChallengeProgress`/`ChallengeManager`/`ChallengeCard` — os gatilhos anexam-se às tabelas sem tocar no código), `community_metrics`, `social_notifications`, todas as outras migrations.

### Fluxos

**Member:** `PointsWidget` na Home e no card da comunidade — saldo atual + "pontos ganhos" (== saldo, sem estorno) + 3 últimas origens + histórico completo sob demanda. RLS entrega só `profile_id = auth.uid()`; `.eq('community_id', foco)` mantém a visão por comunidade. Member não escreve em `point_*`.

**Professional (aba Pontos):** resumo agregado (`points_community_summary`); configurar `recurring_points_per_day` (0–1000); saldo por participante com extrato individual expansível; extrato da comunidade (com o nome de quem ganhou); **conceder pontos** (selecionar participante, 1–1000, motivo opcional). Nunca aparece a si mesma na lista de concessão; nunca há UI de editar/apagar lançamento.

**Master:** só `points_community_summary` (via a aba Comunidade / métricas, quando integrada) — números agregados e quebra por origem. **Sem** `top_earners`, sem `point_ledger`/`point_accounts` individuais (as policies não incluem `is_master()`).

**Conclusão de dia / de desafio / participação recorrente:** o cliente da Fase 6 e o `useDailyMood` **não mudam**; o gatilho no banco verifica comunidade + usuário (do `NEW`), lê o valor configurado, e chama `_award_points` com a `dedupe_key` da ação → 1 lançamento, saldo atualizado pelo trigger de balanço.

### tsc / build / lint

- `npx tsc --noEmit` → **exit 0**.
- `npm run build` (`tsc -b && vite build`) → **exit 0** (aviso de chunk > 500 kB pré-existente).
- `npx oxlint` → **0 erros**, 41 warnings (todos `react(set-state-in-effect)` pré-existentes; os 2 novos em `usePoints.ts` seguem o mesmo padrão de todos os outros hooks).

### Testes — PENDENTES (aguardando `db push` autorizado)

`supabase db push` **não executado**. Sem o banco migrado, os testes funcionais/RLS (por SQL simulando cada perfil, como nas fases 6) e o teste visual no Chrome ficam pendentes. Nenhum `git add`, commit, push ou deploy foi feito.

## Etapa em revisão: Fase 8 — Painel Professional completo (código pronto, migration NÃO aplicada)

Transforma o painel da Nutri numa workspace com abas: **Visão geral** (dashboard), **Participantes**, **Publicações** (moderação), **Desafios**, **Receitas**, **Eventos**, **Círculos**, **Conteúdo**, **Pedidos de ajuda**, **Pontos**, **Métricas**, **Produtos**, **Assinaturas**. Quase tudo é recolocação de managers que já existiam. A única parte nova de banco é a **moderação de publicações** + 2 RPCs de leitura agregada.

### Decisões do usuário
1. Moderação = **Opção B**: RLS esconde o post de todo mundo (exceto Master); ações via RPC, **sem** policy de UPDATE/DELETE no cliente. `Feed.tsx` e `usePosts.ts` **intocados**.
2. **Ocultar** (`hidden_at`) e **Remover** (hard DELETE) — ambos só via `moderate_post`; remoção com confirmação explícita na UI.
3. `community_participants_overview` como RPC de leitura agregada; **sem humor individual**.
4. **Não** alterar `community_metrics`; métricas ampliadas num hook novo compondo fontes reais.
5. Menu lateral: Comunidades e Loja continuam no menu; detalhes em abas dentro do painel.
6. Migration: `supabase/migrations/20260905120000_professional_panel.sql`.

### Migration `20260905120000_professional_panel.sql` (única; NÃO aplicada)

Transacional, idempotente, aditiva, reversível. Nenhuma tabela alterada além do `add column` em `posts`.

- `posts` `+ hidden_at timestamptz` (nullable; NULL = visível) + índice `(community_id, hidden_at, created_at desc)`.
- **`posts_select` reescrita:** `is_master() OR ( hidden_at is null AND ( owns_community(community_id) OR is_community_member(community_id) ) )`. → post oculto some para dona e membros; **Master continua vendo tudo** (dependência da Fase 9). `usePosts.ts`/`Feed.tsx` não mudam — apenas deixam de receber posts ocultos.
- **`moderate_post(p_post_id uuid, p_action text)` → void** — `SECURITY DEFINER`, `search_path=public`, `grant execute to authenticated`. `p_action ∈ {hide,unhide,remove}`. Valida: ação válida; post existe; **`community_id` derivado do próprio post**; `owns_community(community_id)` (senão `not_authorized`). `hide`/`unhide` idempotentes; `remove` = `delete from posts` (cascade limpa `post_comments`/`post_reactions`/`social_notifications`). **Nenhuma policy de UPDATE/DELETE em `posts` para o cliente.**
- **`community_posts_moderation(p_community_id uuid)` → table** — `SECURITY DEFINER`, `stable`, guard `is_master() OR owns_community`. Lista TODOS os posts da comunidade (inclusive ocultos) + autor + `reaction_count`/`comment_count`. Fonte da aba Publicações.
- **`community_participants_overview(p_community_id uuid)` → jsonb** — `SECURITY DEFINER`, `stable`, guard `is_master() OR owns_community`. Por participante ATIVA: `profile_id, full_name, avatar_url, status, joined_at, balance` (de `point_accounts`), `challenges_completed` (de `challenge_completions`), `challenge_days_done` (de `challenge_progress`), `last_activity_at` (max de posts/comentários/progresso de desafio/check-in; `null` se nada). **Não consulta `daily_mood_entries`** — humor individual continua invisível para a Nutri.

### Arquivos

- **Criados:** `supabase/migrations/20260905120000_professional_panel.sql`; `src/types/panel.ts`; `src/hooks/usePostsModeration.ts` (`community_posts_moderation` + `moderate_post`); `src/hooks/useParticipants.ts` (`community_participants_overview`); `src/hooks/useCommunityExtraMetrics.ts` (eventos/receitas/conteúdos/conclusões/joy/ajuda/pontos por `count` + RPC da Fase 7, **sem tocar `community_metrics`**); `src/hooks/useProfessionalDashboard.ts` (compõe `community_metrics` + `points_community_summary` + consultas de ajuda/desafios/receitas/eventos/posts); `src/components/PostsModerationPanel.tsx` (linha de moderação própria — **não usa `PostCard`**, com confirmação inline para remover); `src/components/ParticipantsPanel.tsx` (foto grande, nome, desde, atividade, pontos, desafios); `src/components/ProfessionalDashboard.tsx` (KPIs + blocos, tudo com dado real, cada bloco leva à aba).
- **Alterados:** `src/components/ProfessionalPanel.tsx` (13 abas orientadas por `TABS`, `activeTab` default `'visao'`, `onOpenTab` navega entre abas); `src/components/CommunityMetricsPanel.tsx` (nova seção "Áreas da comunidade" com `useCommunityExtraMetrics` — `community_metrics` **não** alterada); `src/App.css` (bloco `/* Fase 8 */` — `.panel-dashboard*`, `.participant*`, `.moderation-post*` — só tokens existentes; botão de remover em `--danger`, links em `--olive`/`auth-link`); `docs/ESPECIFICACAO_CIRCULA.md`.
- **Intocados:** **`Feed.tsx`**, **`usePosts.ts`** (verificado: zero diff de Fase 8), `PostCard.tsx` (a moderação usa componente próprio → `PostCard` nem ganha props opcionais), `types/post.ts`, `Dashboard.tsx` (o nav do Professional não muda; Comunidades/Loja seguem no menu), `PrimaryNav.tsx`, `App.tsx`, `useDailyMood.ts`/`DailyMoodCard`/`MoodMessageManager`, Mensagens, checkout/billing/produtos (`create_product_order`, `SubscriptionPanel`, `product_*`), `useChallenges`/`useChallengeProgress`/`ChallengeManager`/`ChallengeCard`, `usePoints`/`PointsPanel`/`award_points_manual`, `useHelpQueue`/`HelpQueue`, `EventManager`/`useEvents`, `RecipeManager`/`useRecipes`, `CircleManager`/`useCircles`, `community_metrics` (função), `social_notifications`, branch do Master no `Dashboard.tsx`, todas as outras migrations.

### Fluxo de moderação

Aba Publicações → `usePostsModeration` chama `community_posts_moderation(communityId)` → lista com autor, data, tipo, interações, estado (Visível/Oculta). **Ocultar** → `moderate_post(id,'hide')` → `hidden_at = now()` → o post some de `usePosts`/Feed/Home/Salvos para todos (RLS), **exceto Master**. **Reexibir** → `moderate_post(id,'unhide')`. **Remover** → botão vira "Confirmar remoção definitiva" + "Cancelar" → `moderate_post(id,'remove')` → hard DELETE (cascade). Member/Nutri de outra comunidade chamando qualquer uma → `not_authorized`.

### Fluxo de participantes

Aba Participantes → `useParticipants` → `community_participants_overview(communityId)` → cards com **foto de perfil grande**, nome, "Participa desde…", atividade recente ("ativa hoje" / "há N dias" / "sem atividade há N mês(es)"), **pontos**, **desafios concluídos**, **dias de desafio**. Ordenado por saldo. Sem qualquer dado de humor.

### Métricas

Aba Métricas → `CommunityMetricsPanel` mantém tudo que já tinha (`community_metrics` + humor agregado via `community_mood_overview`) e ganha "Áreas da comunidade": próximos eventos, eventos criados, receitas publicadas, conteúdos publicados, desafios concluídos, momentos de alegria, pontos no período, ajuda pendente, ajuda respondida — todos de tabelas reais que a RLS já libera para `owns_community`/`is_master`.

### Dashboard (Home do painel)

KPIs clicáveis (participantes ativas, novas 30d, ajuda pendente, desafios ativos, pontos 30d, publicações) + blocos: Pedidos de ajuda, Próximos eventos, Receitas recentes, Publicações recentes, Pontos (+ top 3), Engajamento 30d. Cada bloco/KPI navega para a aba correspondente. `useProfessionalDashboard` — 2 RPCs + 5 consultas paralelas, tudo com fonte real.

### Dependências com Fases 5, 6 e 7

- **Fase 5:** aba "Pedidos de ajuda" reaproveita `HelpQueue`/`useHelpQueue` **sem alteração**; o dashboard só conta `help_requests` por status.
- **Fase 6:** aba "Desafios" reaproveita `ChallengeManager` **sem alteração**; `community_participants_overview` lê `challenge_progress`/`challenge_completions` (RLS `owns_community` da Fase 6/7).
- **Fase 7:** `community_participants_overview` e o dashboard leem `point_accounts` e chamam `points_community_summary` — sistema de pontos **não** é alterado; nenhum `point_ledger`/`point_accounts` recriado; sem resgate.

### Riscos

- **R1:** `posts_select` também esconde o post oculto da própria autora (sem aviso). Intencional para moderação; um "seu post foi ocultado" pode vir numa fase futura.
- **R2:** hard `remove` deixa linha órfã em `saved_items` (polimórfica, sem FK). **Inócuo:** `useSavedItems` mapeia os posts resolvidos (a RLS já não devolve o post) → o item some da lista sem erro; `cleanup_saved_items` (RPC já existente) pode varrer.
- **R3:** `community_metrics` (SECURITY DEFINER) conta posts ocultos no `posts_count`. Pequena divergência entre o KPI do dashboard e a contagem real da aba Publicações. Não mexer em `community_metrics` foi decisão do usuário.
- **R4:** `can_view_post()` não checa `hidden_at` → comentários/reações de um post oculto seguem consultáveis por id direto. Nenhum dado novo sensível exposto; a UI nunca entrega id de post oculto a membro.
- **R5:** 13 abas em `.panel-tabs` (`flex-wrap: wrap`, todas visíveis) — densidade visual; confirmar em 320–1440px.

### tsc / build / lint

- `npx tsc --noEmit` → **exit 0**.
- `npm run build` → **exit 0** (aviso de chunk > 500 kB pré-existente).
- `npx oxlint` → **0 erros**, 45 warnings (todos `react(set-state-in-effect)` pré-existentes; +4 nos hooks novos, mesmo padrão de todo o projeto).

### Testes — PENDENTES (aguardando `db push` autorizado)

`supabase db push` **não executado**. Sem o banco migrado, os testes funcionais/RLS (por SQL simulando cada perfil) e o visual no Chrome ficam pendentes. Nenhum `git add`, commit, push ou deploy foi feito.

### Correção R4 (Fase 8) — post oculto invisível também por ID direto

Migration aditiva `supabase/migrations/20260905130000_hidden_post_visibility.sql` (**NÃO aplicada** — aguardando autorização de `db push`).

`CREATE OR REPLACE` de duas funções existentes (mesma assinatura, `STABLE SECURITY DEFINER`, `search_path='public'`):
- **`can_view_post(uuid)`** — passa a exigir `p.hidden_at IS NULL` no ramo dona/membro (espelha `posts_select`); `is_master()` continua vendo tudo (dependência Fase 9). Usada pelas policies `post_comments_select`, `post_reactions_select` (USING) e `saved_items_insert` (CHECK, ramo `item_type='post'`).
- **`can_participate_in_post(uuid)`** — passa a exigir `p.hidden_at IS NULL`. Usada pelas policies `post_comments_insert` e `post_reactions_insert` (CHECK).

**Nenhuma policy alterada** (elas já chamam essas funções). Nenhuma tabela alterada. Arquitetura de moderação intocada — a Nutri administra o post oculto pela RPC `community_posts_moderation` (SECURITY DEFINER, fora dessas policies). R1 e R3 não tocados. `Feed.tsx` e `usePosts.ts` não tocados (correção 100% no banco). `tsc`/`build`/`oxlint` verdes (nenhum código React mudou).

Resultado esperado: Member não lê conteúdo/comentários/reações de post oculto nem por ID direto, e não consegue comentar/reagir/salvar; a dona segue administrando pelo fluxo de moderação; comunidade alheia bloqueada.

## Etapa em revisão: Fase 9 — Painel Master (visão de plataforma) — código pronto, migration NÃO aplicada

Substitui o espelho por-comunidade do Master (que renderizava `Feed` + todos os managers + `MemberList` de cada comunidade) por um **MasterPanel** com 4 abas alimentado por RPCs agregadoras. Master = visão da plataforma; Professional = administração da própria comunidade; Member = experiência individual. Sem sobreposição.

### Decisões do usuário
1. **RLS só Tier 1**: remover `is_master()` do SELECT de `help_requests` e `help_request_replies`. Sem varredura Tier 2 (posts/joy/challenge_*/community_members/can_view_*) nesta fase.
2. `platform_communities` pode expor nome/avatar da comunidade e nome/avatar da profissional responsável (informação administrativa, não conteúdo de usuária).
3. 4 abas: **Visão Geral · Comunidades · Profissionais · Plataforma**. "Usuários" só como bloco agregado dentro de Visão Geral; sem tela individual.
4. Sem `platform_growth` / séries históricas nesta fase.
5. Migration `20260906120000_master_panel.sql`.
6. Índices aditivos baratos, diretamente ligados às consultas do Master.

### Migration `20260906120000_master_panel.sql` (única; NÃO aplicada)

Transacional, idempotente, aditiva, reversível. **Sem tabelas, sem views.**

**(1) RLS Tier 1** — `drop policy` + `create policy` de `help_requests_select` e `help_request_replies_select` **sem o ramo `is_master()`**. `owns_community` + regra de audiência da Fase 5 preservados → Member e a dona da comunidade não mudam; o Master deixa de ler texto/histórico de pedido de ajuda.

**(2) 5 índices aditivos** (`if not exists`, btree): `posts(created_at)`, `challenge_completions(completed_at)`, `point_ledger(created_at)`, `community_members(status, joined_at)`, `profiles(role, created_at)` — cada um sob um filtro de range/igualdade que as RPCs varrem SEM filtro de comunidade.

**(3) 3 RPCs** — todas `SECURITY DEFINER`, `stable`, `set search_path to 'public'`, 1ª linha `if not public.is_master() then raise 'not_authorized'`, `grant execute … to authenticated`, **sem parâmetro do cliente**, retorno `jsonb` **só com agregados**:
- `platform_overview()` — ~35 contagens/somas da plataforma inteira: comunidades/ativas/novas, profissionais/ativas/novas, membros/novos (distinct count), usuárias total/7d/30d/por papel, publicações total/30d, receitas/conteúdos publicados, eventos/próximos, desafios/ativos/conclusões/30d/dias marcados, ajuda por status (**contagem**), momentos de alegria total/30d, respostas de check-in, **pontos distribuídos total/30d** (`sum(point_ledger.amount)` — sem `profile_id`), assinaturas por status (plataforma e comunidade), trials terminando em 7d, catálogo de planos, split de receita.
- `platform_communities()` — 1 objeto por comunidade: id/name/slug/cover/created_at + **owner** (id/full_name/avatar_url) + `subscription_status` + membros ativos/novos 30d + posts 30d + conclusões 30d + pontos 30d/total + ajuda pendente (contagem) + `last_activity_at` (max de timestamps — sem `profile_id`).
- `platform_professionals()` — 1 objeto por profissional: id/full_name/avatar_url/created_at + `platform_active` + status de assinatura de plataforma + nº de comunidades + `count(distinct profile_id)` de membros + posts 30d + `last_activity_at`.

**Nenhuma RPC** consulta `daily_mood_entries`, retorna `profile_id` de membro, saldo individual, `point_ledger` linha a linha, texto de post/comentário/ajuda/alegria, ou conversa.

### Arquivos

- **Criados:** `supabase/migrations/20260906120000_master_panel.sql`; `src/types/platform.ts`; `src/hooks/usePlatformOverview.ts` / `usePlatformCommunities.ts` / `usePlatformProfessionals.ts`; `src/components/MasterPanel.tsx` (casca 4 abas; `usePlatformOverview` uma vez, compartilhado por Visão Geral e Plataforma), `MasterDashboard.tsx`, `MasterCommunitiesPanel.tsx`, `MasterProfessionalsPanel.tsx`, `MasterPlatformPanel.tsx`.
- **Alterados:** `src/components/Dashboard.tsx` — branch `profile.role === 'master'` passa a renderizar `<MasterPanel />` (−~65 linhas de espelho); removidos 7 imports que só serviam ali (`QuestionBankManager`, `ChallengeManager`, `CircleManager`, `EventManager`, `ContentManager`, `CheckinManager`, `EngagementCommandManager`); `+ import MasterPanel`. `src/App.css` — bloco `/* Fase 9 */` (`.master-*`, só tokens existentes). `docs/ESPECIFICACAO_CIRCULA.md`.
- **Intocados:** `Feed.tsx`, `usePosts.ts`, `useDailyMood`/`DailyMoodCard`, `useJoyMoments`/`JoyMoment*`, `RecipeManager`/`useRecipes`, `HelpQueue`/`useHelpQueue`, `ChallengeManager`/`useChallenges`/`useChallengeProgress`, `PointsPanel`/`usePoints`/`award_points_manual`, `ProfessionalPanel` e as 13 abas, `community_metrics`/`points_community_summary`/`moderate_post`/`community_posts_moderation`/`community_participants_overview`, `PrimaryNav.tsx`, `App.tsx` roteamento, Mensagens, checkout/billing/produtos. Nenhuma tabela alterada.

### Dados disponíveis para o Master

Agregados de: comunidades (contagem, ativas, novas, capa/nome, profissional responsável, membros, atividade, assinatura), profissionais (contagem, comunidades administradas, membros somados, assinatura, atividade), usuárias (contagens por papel, novas 7d/30d), publicações (contagem, 30d), receitas/conteúdos (contagem, comunidades com receitas), eventos (contagem, próximos), desafios (total, ativos, conclusões, dias marcados, 30d), pedidos de ajuda (**contagem por status**), momentos de alegria (**contagem**), check-ins (**contagem**), pontos (**total e por comunidade e 30d — soma agregada**), billing (assinaturas por status, trials a expirar, planos, split).

### Dados BLOQUEADOS para o Master

Texto de post/comentário; `daily_mood_entries` e qualquer humor individual; `point_accounts`/`point_ledger` individual, saldo de uma usuária, histórico individual (A1 preservado); **texto e resposta de pedido de ajuda** (Tier 1); `messages`/`conversations`; `social_notifications`; texto de momento de alegria; `challenge_progress` linha a linha; `profile_id` de membro; tela de investigação individual de usuária (não existe drill-in no MasterPanel); alteração de qualquer dado (todas as RPCs são read-only).

### RLS

| Objeto | Antes | Depois |
|---|---|---|
| `help_requests_select` / `help_request_replies_select` | inclui `is_master()` | **sem `is_master()`** (Tier 1); Member/dona inalterados |
| `daily_mood_entries`, `point_accounts`, `point_ledger`, `messages`, `conversations`, `social_notifications` | já sem `is_master()` | inalterados |
| `posts`, `joy_moments`, `challenge_progress`, `challenge_completions`, `community_members`, conteúdo de comunidade, `profiles`, `subscriptions`, billing | incluem `is_master()` | **inalterados** (Tier 2 adiado por decisão — residual latente registrado no risco MR2) |
| 3 RPCs novas | — | guard `is_master()`; `search_path='public'`; sem GRANT a `anon` |

### Riscos

- **MR1:** Tier 1 altera 2 policies da Fase 5. Member/dona verificados inalterados (só o ramo Master some). Baixo.
- **MR2 (residual aceito):** Master ainda tem `is_master()` na RLS de `posts`/conteúdo/`profiles`/etc. → acesso latente a linhas via API crua (não pela UI). Tier 2 adiado por decisão 1. Registrar.
- **MR3:** Master perde a navegação de conteúdo das comunidades (objetivo declarado da fase).
- **MR4:** `platform_overview` roda ~35 subconsultas num statement; instantâneo no volume atual; 5 índices cobrem as janelas 30d; `community_content`/`community_events`/`joy_moments`/`checkin_responses` sem índice dedicado → seq scan aceitável para um painel de admin de carga rara (índice futuro se necessário).
- **MR5:** `platform_overview.plans` devolve o catálogo de planos (preços) ao cliente Master — config de negócio da plataforma, apropriada ao Master, sem PII.

### tsc / build / lint

- `npx tsc --noEmit` → **exit 0** (7 imports órfãos removidos do `Dashboard.tsx` sem erro de `noUnusedLocals`).
- `npm run build` → **exit 0** (aviso de chunk > 500 kB pré-existente).
- `npx oxlint` → **0 erros**, 48 warnings (todos `react(set-state-in-effect)` pré-existentes; +3 nos hooks `usePlatform*`, mesmo padrão do projeto).

### Testes — PENDENTES (aguardando `db push` autorizado)

`supabase db push` **não executado**. Sem o banco migrado, os testes funcionais/RLS (por SQL simulando Master/Professional/Member) e o visual no Chrome ficam pendentes. Nenhum `git add`, commit, push ou deploy.

## Etapa concluída: FASE 10 — Home consolidada + Conquistas

Consolida a Home da usuária Member (e Professional, que compartilha o destino "Início") num fluxo completo e acolhedor, reaproveitando os módulos das Fases 3–7 + dois blocos novos (cabeçalho de comunidade, faixa de conquistas). **Sem banco: nenhuma migration, nenhuma tabela, nenhuma view, nenhuma alteração de RLS.** `Feed.tsx` e `usePosts.ts` intocados. Validada ao vivo no Chrome nos 3 perfis (Member, Professional, Master).

### Banco
A Fase 10 é 100% UI + hooks derivados. Leituras só de linhas que o perfil já podia ler pela RLS existente: `point_accounts` (`profile_id = auth.uid()`), `challenge_completions` / `challenge_progress` (ramo `profile_id = auth.uid()`), `joy_moments` (`is_community_member`), `community_members` (ramo `profile_id = auth.uid()`), `community_challenges` (`is_community_member`), `community_events`, e `profiles` da profissional responsável (policy `profiles_select` via `shares_active_community`). Nenhuma escrita, nenhuma RPC nova, **nenhuma migration**.

### Arquivos criados (5)
- `src/types/achievement.ts` — `Achievement`, marcos fixos: pontos [50/100/250/500/1000], desafios [1/3/5], dias [10/30], tempo [1/6/12 meses].
- `src/hooks/useAchievements.ts` — deriva as conquistas de 6 consultas minúsculas (saldo, conclusões, dias, momentos de alegria próprios, `joined_at`). As contagens usam `GET` + `count: 'exact'` + `.limit(0)` (ver "Correção 503" abaixo). Expõe `unlocked`, `unlockedCount`, `nextAchievement` (a mais perto de completar), `pointsBalance`.
- `src/hooks/useRailSummary.ts` — reaproveita `useAchievements` (saldo + nº de conquistas) + 1 consulta do próximo evento. Só para o trilho ≥1280px.
- `src/components/AchievementsStrip.tsx` — faixa "Suas conquistas": selos desbloqueados + próxima conquista com barra de progresso. Some se não houver nada.
- `src/components/HomeCommunityHeader.tsx` — cabeçalho enxuto: capa (aspect-ratio), logo do Círcula, nome, profissional responsável (avatar + nome) e nº de participantes. A contagem vem da prop `memberCount` (RPC `community_member_count`, via `useCommunity.memberCounts`) — **nunca** de `community_members.length` (que a RLS reduz à própria linha para uma Member). A responsável vem do embed quando existe (dona/Master) ou de um `GET profiles?id=eq.<owner_id>` quando não (Member) — sempre filtrado pelo `owner_id` exato, sem fallback. Somente leitura.

### Arquivos alterados (7 + doc)
- `src/components/HomeToday.tsx` — monta `HomeCommunityHeader` no topo (com `memberCount={memberCounts[focusCommunity.id]}`); reordena para **humor → alegria → ajuda** (abrir com leveza); monta `AchievementsStrip` logo após `PointsWidget`.
- `src/components/HomeHighlights.tsx` — faixa "Receitas" passa a usar **`RecipeCard`** (identidade visual própria) num grid `home-recipe-row`; novo bloco **"Conteúdo para você"** (`ContentCard`, `type <> 'recipe'`, publicado, limite 3, some se vazio).
- `src/components/HelpRequestSection.tsx` — copy acolhedora ("Quer conversar sobre alguma coisa?", "Não se sinta sozinha. Você pode pedir ajuda quando precisar."); nota positiva quando não há pedidos ("Dias difíceis passam…"). Estrutura e hooks intactos.
- `src/components/DashboardRail.tsx` — resumo Fase 10: chip de **pontos**, chip de **conquistas**, cartão **próximo evento**. Continua leve; só ≥1280px.
- `src/components/Dashboard.tsx` — `useRailSummary(railActive ? … : null)` (só member/professional, com comunidade, em `activeNav === 'inicio'`); passa `pointsBalance`/`achievementsCount`/`nextEvent` ao `DashboardRail` e `memberCounts` ao `HomeToday`. **Branch do Master não muda.**
- `src/hooks/useCommunityExtraMetrics.ts` (Fase 8) — 6 contagens migradas de `head: true` para `GET` + `.limit(0)` (correção 503).
- `src/hooks/useProfessionalDashboard.ts` (Fase 8) — 1 contagem (`help_requests`) migrada de `head: true` para `GET` + `.limit(0)` (correção 503).
- `src/App.css` — bloco `/* Fase 10 */`: `.home-community-header*`, `.achievements-strip`/`.achievement-badge`/`.achievement-next*`, `.home-recipe-row`, `.help-section-note`, `.rail-summary*`/`.rail-next-event*`; fotos maiores **só nos contextos da Home** (`.home .post-avatar` 48, `.home .joy-card-avatar` 44, `.dashboard-header .dashboard-user-avatar` 42 — **Feed não muda**); animação do emoji de humor (`transform` no hover/`:focus-visible`, `@keyframes moodPop` na seleção, `@media (hover: none)` para toque); hover do logo (`.brand-icon`/`.brand-logo` → `scale(1.06) translateY(-1px)` + sombra, volta suave; `:active` no toque); **`@media (prefers-reduced-motion: reduce)`** desligando as animações novas.

### Arquivos intocados
`Feed.tsx`, `usePosts.ts` (0 alteração de Fase 10 — confirmado por diff), `useDailyMood.ts` (privacidade do humor intacta), `useJoyMoments.ts`, `useHelpRequests.ts`, `useRecipes.ts`, `useChallenges.ts`/`useChallengeProgress.ts`, `usePoints.ts`/`PointsWidget.tsx`/`PointsHistory.tsx`, `useContent.ts`, `DailyMoodCard.tsx` (efeito é só CSS), `ChallengeCard.tsx`/`EventCard.tsx`/`ContentCard.tsx`/`JoyMomentCard.tsx`/`JoyMomentsSection.tsx`, `ProfessionalPanel` e as 13 abas, `MasterPanel`/`platform_*`, `PrimaryNav.tsx`, todas as migrations e RPCs.

### Estrutura final da Home (coluna principal)
1. Saudação → 2. **Cabeçalho da comunidade** (capa+logo+nome+Nutri+nº) → 3. **Como você está hoje?** (emojis interativos) → 4. **Momento de alegria** → 5. **Pedido de ajuda** (copy acolhedora) → 6. **Seu desafio** → 7. **Seus pontos** → 8. **Suas conquistas** → 9. Próximos eventos → 10. **Receitas** (RecipeCard) → 11. **Conteúdo para você** → 12. Pergunta do dia / Comando / Check-in → 13. Publicações recentes → 14. Seus círculos + Sugeridos → 15. Atividade recente → 16. Atalhos.
Blocos 9–13 só aparecem com dado real (comportamento herdado do `HomeHighlights`).

### Trilho direito (≥1280px)
Resumo (pontos + conquistas), próximo evento, cartão da comunidade, "Ir rápido", nota. Mobile: sem trilho — o conteúdo equivalente já está na coluna principal (pontos = bloco 7, conquistas = 8, eventos = 9).

### Conquistas
Derivadas, marcos fixos. Selos desbloqueados como pílulas; "Próxima: X — a de N" com barra de progresso (incentivo). Nunca vazio de forma triste — se nada desbloqueado, mostra a mais próxima com barra.

### Correção 1 — Cabeçalho da comunidade (achado na validação do Chrome)
Para uma Member, o cabeçalho mostrava "1 mulher" e omitia a profissional responsável, porque derivava tudo de `community.community_members` — a policy `community_members_select` (`is_master() OR profile_id = auth.uid() OR owns_community(...)`) devolve só a própria linha da Member. O componente estava certo; a **fonte** era incompleta. Corrigido: contagem passa a vir da RPC `community_member_count` (prop `memberCount`); a responsável, do embed quando disponível ou de um `GET profiles?id=eq.<owner_id>` (só `id,full_name,avatar_url`; policy `profiles_select` já libera via `shares_active_community`). Nenhum valor fixo, nenhuma "primeira comunidade", nenhum fallback para outra pessoa. Sem migration, sem RLS.

### Correção 2 — 503 em `HEAD` + `count=exact` (achado na validação do Chrome)
Toda contagem feita com `.select('*', { count: 'exact', head: true })` (requisição `HEAD`) voltava **503** do gateway do Supabase (`sb-gateway-version: 1`), silenciosamente — o `supabase-js` engolia o erro e o contador caía em 0. Reproduzido: `fetch` cru com a mesma URL, filtros, `Prefer: count=exact` e headers, em paralelo → **200** sempre. Não é índice, timeout, RLS, volume nem consulta pesada — é comportamento do gateway para `HEAD`. Afetava **Conquistas** (`useAchievements`) e **Métricas da Fase 8** (`useCommunityExtraMetrics`, `useProfessionalDashboard`). Corrigido trocando `head: true` por `GET` + `count: 'exact'` + `.limit(0)`: zero linhas trafegadas, total exato no `Content-Range` (`res.count`), `GET` 100% confiável neste gateway. RLS idêntica, nenhuma permissão aberta, nenhum `service_role`, sem mascarar o erro. `grep "head: true"` em `src/` → 0.

### tsc / build / lint
`npx tsc --noEmit` → exit 0 · `npm run build` → exit 0 (aviso de chunk pré-existente) · `npx oxlint` → 0 erros, 50 warnings (todos `react(set-state-in-effect)` pré-existentes — baseline, nenhum novo).

### Testes no Chrome
Validado ao vivo (`http://localhost:5173/circula/`) nos 3 perfis:
- **Member** — cabeçalho com nome + responsável + foto + "2 mulheres"; emojis de humor com hover/seleção/persistência; copy do pedido de ajuda; RecipeCard + "Conteúdo para você"; Conquistas com números reais ("2 de 10"); trilho ≥1280px; **69 requisições, todas 200/206, zero 503**; console limpo.
- **Professional** — cabeçalho correto (caminho do embed); trilho "2 conquistas"; Painel → Métricas com valores reais (ex.: "Desafios concluídos 1"), humor **agregado sem identificar quem**; contagens via `GET …&limit=0` → 206; console limpo.
- **Master** — `MasterPanel` 4 abas, só agregados via `rpc/platform_*`; nenhuma leitura direta de dado privado individual; nenhum arquivo alterado no seu caminho; console limpo.

Responsividade 320/375px: não renderizável neste ambiente (a janela do Chrome não reduz abaixo de ~1366px); 1280/1440px OK, sem overflow. A correção não altera CSS do cabeçalho (só render condicional + uma consulta).

Sem `supabase db push`, sem dados de teste criados, sem alteração de RLS.
